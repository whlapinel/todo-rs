Ranked easiest → hardest (re-ranked 2026-08-15 after the item_series redesign completed). Resolved items keep their original strikethrough history at the bottom of this file (unchanged) rather than moving to archived-issues.md, since none of them fully close out — see notes. Exceptions, closed out today: "Calendar view of tasks should show dimmed and crossed out if complete" (fixed), "Dashboard should not show simple list items" (fixed), "hide the offset instead of 'no offset'" (withdrawn, not being done), "Task series can't be assigned" (fixed, bundled with a new points-for-series feature — moved to archived-issues.md, not just struck through, since it's the first fully-closed item in this file), and, as of 2026-08-16, "Batch add UI should be available in task details add sub-task screen" (fixed, also moved to archived-issues.md) — see their entries below. As of 2026-08-19: the former ranked item 2 ("Add a link to the series from an item's detail page... skipped indicator... unskip action") is now fully closed too — it was folded into `docs/unify-virtual-materialized-occurrences-plan.md`, which finished all four of its stages (A–D) today — and has been moved to archived-issues.md; the remaining ranked items below are renumbered accordingly. Also as of 2026-08-19: the virtual-occurrence-completion-doesn't-award-points bug (previously an unranked, un-numbered entry near the bottom of the original issue history) is fixed (`src/service/team_items.rs`) and moved to archived-issues.md.

1. Contrast issue for select box in series' "Children Template" field: reported as white text on white background. **Flag: possibly already resolved, re-verify visually before scoping work.** The current markup (`templates/project_item_series/new_page.html`) already uses this app's standard dark-mode-aware select classes (`bg-white ... text-gray-900 ... dark:bg-white/5 dark:text-white`), identical to every other `<select>` in the codebase — no obvious white-on-white bug in the source. This field was added in the item_series redesign's "gap 3" (children carry-forward via `template_item_id`, committed 2026-08-15) — possible this issue was filed against an earlier build, or the bug is browser/theme-specific and not visible from source alone. Check live in both themes before treating as a code task.

2. "Seems like complete-task should get a dedicated handler rather than using update-item and bearing the complexity of json parsing." **Medium-hard**: today every screen's checkbox toggle PUTs `{"complete": "..."}` through the same general `update_*_form`/`update_item` path every other field edit uses, and per CLAUDE.md's row-editing convention that handler renders three concatenated fragments (row+fields+view) with each caller `hx-select`-ing its own piece — a dedicated toggle-only endpoint would need to preserve that three-fragment contract (or deliberately narrow it) across all six dedicated screens (`project_tasks`, `project_events`, `project_simple_lists`, template children, plus both personal/team dispatch inside each). Real refactor, not a quick win, but self-contained (web UI layer only, no schema/Smithy change implied).

3. We had index creation in the wrong place, before the migrations that added the columns those indexes are on — only the `source_event_id` one was confirmed fixed, but there may be others in the same wrong position. **Medium**: needs an audit pass through every file in `src/storage/migrations/` to check each `CREATE INDEX`'s ordering against the column-adding migration it depends on, then split/reorder any offenders — mechanical once found, but requires reading the whole migrations directory carefully since the fix is only as good as the audit.

4. Need a plan for deleting old data to keep the DB from growing infinitely (completed recurring items, activity log rows), likely via a user-configurable retention/row-limit. **Hard**: this is a design task before it's an implementation task — no doc exists sketching an approach yet (unlike the other open features here, which mostly already have a written plan). Worth its own planning pass (retention policy shape, whether it's global config or per-user, whether "delete" or "archive") before touching code.

5. Potential long-term issue: are we duplicating code unnecessarily between `items.rs`/`team_items.rs`? Risk of drift. **Not a discrete task — architectural observation, lower priority than it looks.** CLAUDE.md's own Storage/Web UI sections already describe why this split still exists (`team_items.rs` owns points/assignment/completion-guard logic Task-shaped personal items don't need) and that `project_items.rs`'s two-way dispatch is the seam meant to contain the duplication rather than eliminate it. Worth revisiting only if a specific new piece of logic is about to be duplicated a third time — not something to schedule on its own.

---

Flagged as unclear or possibly stale — needs a decision from the user rather than being ranked as a task:

- "Should events have 'save as template' ability? Not sure we need event templates... maybe templates should be called task templates." Still an open product question, sharpened (not answered) by the item_series redesign: series-level children carry-forward (`template_item_id`) is now explicitly Task-only, and `event_type` (the mechanism standalone Events use to auto-trigger a template) is now unconditionally disallowed on any series — but standalone (non-series) Events can still "Save as template" today via `project_events/`'s existing button (CLAUDE.md's Screens section). Worth deciding directly with the user rather than scheduling as an implementation task: keep Event templates as-is, or narrow the whole templates concept to Tasks as originally suggested?
- "As documented elsewhere, the select box 'Move Under' was deleted due to ruining the mobile layout..." — this isn't an outstanding task, it's a historical note explaining a past deliberate removal (confirmed: `archived-features.md` already documents the promote/subordinate feature this refers to). Recommend deleting this line from issues.md entirely, or converting it into a real feature request ("re-add a Move Under picker using a proper Tailwind Plus component") if that's actually still wanted.
- "Made some changes to tasks rows and task calendar, need to add same changes to other UIs that apply e.g. personal events and team events." — the phrasing predates the Project-abstraction collapse (CLAUDE.md's Web UI section): "personal events and team events" haven't been separate screens for a while now — there's just one project-scoped `project_events/` that dispatches on `project.team_id` internally. Before scheduling this, needs a re-check of what specific row/calendar changes were meant and whether `project_tasks/`/`project_events/` have actually diverged since — as currently worded it's too vague to size.

---

## Original issue history (unchanged, resolutions inline)

- Should events have "save as template" ability? Not sure we need event templates, but perhaps. Maybe templates should be called task templates to clarify the narrower purpose if we decide to rule out event templates.
- Potential long term issue: are we duplicating code unnecessarily between items and team items? risk of code drift, and different logic when it should be consistent between the two repos?  Should consolidate where feasible. 
- ~~Potential major bug: it appears that when marking recurring items complete, only the parent item is deleted, which would orphan all child items.~~ ~~Furthermore, not sure I want to just delete the completed item. Might want to keep it actually. When we show completed items, any recurring item doesn't show up because it was deleted.~~ Investigated and fixed. The "orphaned children" half was already not actually happening — `clone_children` (`src/service/items.rs`) re-parents every descendant onto the new occurrence (recursively, recomputing each one's `due_date` from its own `due_offset_days` against the new anchor) and deletes the old descendants as it goes, before the old parent itself is ever touched; this was already true going into this task and wasn't a live bug. The real gap was the second point: `update_item`/`update_team_item`'s recurrence branch unconditionally `repo.delete`d the just-completed parent row after cloning its children onto the successor, so a completed recurring item could never appear anywhere, including under "Show completed" — there was nothing left in the DB to show. Fixed by persisting the completed occurrence instead of deleting it (`repo.update`/`repo.update_team_item`, same row id, `complete: true`), so it now shows up like any other completed item wherever the existing `show_complete` toggle already applies (Tasks/Team Tasks/Events/Team Events list pages, and the dashboard's due-date presets, all of which already had this filter and needed no changes). One new failure mode this opened up: since a completed item's read-only detail view keeps a checkbox that can flip it back to incomplete, and the archived row still has its original `recurrence`/`recurrence_basis`, re-completing it (e.g. via the checkbox) would otherwise re-fire `next_recurrence` a second time and spawn a duplicate "next occurrence" alongside the one already created by the original completion. Closed by a new `archive_recurrence` helper (`src/service/items.rs`) that clears `recurrence`/`recurrence_basis` on the archived row right before it's persisted — `next_recurrence` requires a parseable pattern to fire at all, so the archived row is permanently inert as a recurrence source even if toggled complete/incomplete/complete again; verified live via the JSON API that doing exactly that leaves the item count unchanged (no duplicate spawned). Updated the two existing unit tests that asserted the old `expect_delete` behavior to assert `expect_update`/`expect_update_team_item` with a cleared recurrence instead (`cargo test`, 96/96 passing); live-verified end-to-end against a scratch server: completing a recurring top-level item with an offset child leaves the old row in the DB as `complete=1, recurrence=NULL, parent_item_id=NULL`, the new occurrence as `complete=0` with the advanced due date and the re-parented child, and the old child row gone entirely. **Note (2026-08-15): moot for new items** — item-level recurrence (the entire mechanism this fix patched) was retired outright in the item_series redesign's Stage 10 (`8797ac9`); this history is kept for context but the code paths it describes (`next_recurrence`, `archive_recurrence`) no longer exist.
- ~~Dashboard should not show simple list items - only events and tasks~~ Fixed 2026-08-15: added `.filter(|di| di.item.kind() != ItemKind::Simple)` to the candidate list in `render_rows` (list view) and an equivalent early-`continue` in `build_calendar_days` (calendar view), both `src/web_ui/project_dashboard.rs`. Live-verified against a scratch server: a Simple item with no due date no longer appears under the dashboard's "All" preset, while a dated Task still does; calendar view unaffected (200 OK, no errors). `cargo build`/`cargo test` (345/345) clean.
- ~~Not a bug but let's hide the offset instead of putting "no offset" for template child items if offset is none.~~ Withdrawn 2026-08-15 — decided not worth doing, removed from the ranked list at the user's request.
- Need a plan for deleting old data to keep db from growing infinitely. Maybe (probably) this is a user configuration, and set hard limits on number of items per user. Including but not limited to:
    - Completed recurring items (e.g. daily) have the potential to add a lot of rows to the db 
    - The activity log has the same potential
- We had index creation in the wrong place, before the migrations were run that added the columns those indexes would be set on. I only moved the one for source event id because that was causing an error but I think a bunch of them are in the wrong place and should be within their respective migration itself.
- ~~Batch add UI should be available task details add sub-task screen~~ Fixed 2026-08-16, moved to archived-issues.md (same treatment as "Task series can't be assigned" above, since it's fully closed with no remaining gaps).
- As documented elsewhere, the select box "Move Under" was deleted due to ruining the mobile layout and being way too wide, and having contrast issues within the dropdown (light text on light background).
- Made some changes to tasks rows and task calendar, need to add same changes to other UIs that apply e.g. personal events and team events.
- Seems like complete-task should get a dedicated handler rather than using update-item and bearing the complexity of json parsing
- ~~In the case where a user materializes an occurrence and then deletes it, I think the series occurrence should also be deleted... I'd also like to have an indicator wherever a task or event is skipped, and would like an "unskip" UI...~~ Fully closed 2026-08-19 (folded into `docs/unify-virtual-materialized-occurrences-plan.md`, all four stages complete) — moved to archived-issues.md, same treatment as "Task series can't be assigned"/"Batch add UI" above.
- Contrast issue for select box in series: Children Template - white text on white background. Select box possibly needs Tailwind UI component instead of plain input **Note (2026-08-15): possibly already resolved — see ranked item 2 above.**
- ~~Task series can't be assigned - needs to be able to be assigned~~ Fixed 2026-08-15, bundled with a new points-for-series feature request — moved to archived-issues.md (first fully-closed item in this file, so struck through here and archived rather than kept as an inline note like the other resolved entries above).
- ~~Calendar view of tasks should show dimmed and crossed out if complete~~ Fixed 2026-08-15: `CalendarTaskEntry` gained a `complete: bool` field (`src/web_ui/project_tasks/templates.rs`, populated at all three push sites in `build_calendar_days`, `src/web_ui/project_tasks/mod.rs`), and `templates/project_tasks/calendar_page.html`'s entry rendering now applies `opacity-50`/`line-through` to a completed task's row (name and time label both), reusing existing Tailwind utility classes already present in the compiled CSS — no `task web-styles` rebuild needed. `cargo build`/`cargo test` (345/345) clean.
- for both virtual and materialized occurrences of a series, task list should only show the current one.
- ~~Clicking on a project from the projects selector page doesn't populate sidebar context for some reason. Refreshing the page will populate it.~~ Fixed 2026-08-16: root cause was `hx-boost`'s default `#page`-only swap — `base.html`'s `<body>` scopes boosted navigation to `#page`'s innerHTML so the header/sidebar (`{% block header %}{{ nav_html|safe }}{% endblock %}`, built server-side per request from `ActiveContext`) stays untouched across a boosted nav, by design (CLAUDE.md's Web UI section). The projects list page's project-row link (`templates/projects/list_page.html`) explicitly set `hx-target="#page" hx-select="#page"`, so clicking into a project only ever swapped `#page` — the nav sitting outside it kept rendering `ActiveContext::None` until a real page load rebuilt it. Sidebar/header navigation itself always avoids this by setting `hx-boost="false"` on every link that changes context (`templates/nav.html`'s `<header hx-boost="false">`, `nav_sidebar_inner.html`'s per-link `hx-boost="false"`) so it falls through to a full page load instead of a boosted partial swap. Fixed by giving the project-row link the same `hx-boost="false"` treatment. Found and fixed the same bug in reverse too — the "Back to projects" links on `project_tasks/`, `project_events/`, and `project_simple_lists/`'s list pages (project context → no context) had the identical boosted-partial-swap issue and got the same `hx-boost="false"` fix. `cargo build` clean.

- ~~I think series id should be added as column to items so that items have an easy way to link to the series they are a materialized occurrence of.~~ Added 2026-08-19 as Stage A of `docs/unify-virtual-materialized-occurrences-plan.md` — `items.series_id`, set once at materialization by `get_or_materialize_occurrence`, never user-settable. Consumed by the UI as of that same plan's Stage B (series-link on a materialized item's detail page) — moved to archived-issues.md along with the rest of that now-fully-closed plan.
- ~~Major bug - marking an item complete advances the cursor to the item. marking an item incomplete after marking it complete will not restore the cursor back to its original position. So current will remain the occurrence after the one marked complete. Fix -- if item being marked incomplete is cursor, move cursor to previous item?  And disallow marking non-cursor series occurrences incomplete?~~ Fixed 2026-08-19: two new functions in `src/service/item_series.rs`, mirroring the existing `validate_completable`/`record_task_completion` pair. `validate_uncompletable` (pre-persistence, called from `update_project_item` when the request is a genuine complete→incomplete transition) rejects uncompleting anything but the series' most recently *completed* occurrence — `cursor_date` holds that occurrence's own date directly, so this is a plain equality check, not a self-heal walk; if the cursor's own occurrence is itself marked exdate (the most recent settlement was a Skip, not a completion), the request is rejected outright rather than silently walking further back to an earlier completion, since a Skip needs its own explicit "unskip" (still a separate open item above) before anything under it can be reopened. `record_task_uncompletion` (post-persistence) then actually restores the cursor — one recurrence step back via `recurrence::retreat_once` (new, mirrors `advance_once`), or cleared to `None` via a new guarded `ItemSeriesRepo::clear_cursor` if the occurrence being uncompleted was the series' very first (anchor) one. Both are gated on an actual fetched-and-checked `was_complete`, not on `!params.complete` alone — unlike `record_task_completion`'s forward-only `advance_cursor` (safe to call on every redundant re-completion), `retreat_cursor`/`clear_cursor` are not idempotent against being called on a plain edit of an already-incomplete item. `cargo test` (367/367) clean.
  - **Same-day follow-up, "unify completion-undo":** while fixing the above, found the codebase already had two divergent ways to undo a completion — the checkbox (which the fix above covers) and the activity feed's manual "Undo" button (`undo_activity_log_entry`), which only ever reversed points and never touched the item's own `complete` flag or the series cursor (a deliberate design from when completing a legacy recurring item deleted the row outright — moot now that materialized occurrences are never deleted). Unified them: `undo_activity_log_entry` (and a new project-scoped sibling, `undo_project_activity_log_entry`) now delegates into `project_items::update_project_item`'s own `complete: false` path after reversing points, via a shared `reverse_and_reopen` helper — the exact same path the checkbox uses, so points reversal, series-cursor restore, and the completion-guard rules all apply identically regardless of which UI triggered the uncomplete. Also extended activity logging to *any* top-level completion, not just points-bearing team ones (`team_items.rs`'s points gate on `log_activity` dropped — points-awarding stays gated, logging doesn't; `items.rs`'s personal-item path gained the same logging/reversal block team items already had, `points_delta: 0`, `team_id: None`), so a personal project's own Activity page now shows and can undo any completion too, not just team ones. Required `activity_log.team_id` to go nullable (`ActivityLogTeamIdNullable` migration, rebuild-and-copy since SQLite can't drop a `NOT NULL` constraint in place) and `ActivityLogEntry.team_id`/`ActivityLogRepo::log_activity`'s `team_id` param to become `Option`. Legacy team-scoped `ListTeamActivityLog`/`UndoActivityLogEntry` Smithy ops (`prl teams activity`/`undo-activity`, their MCP tools) untouched — no Smithy/codegen change, since the web UI's activity feed already called the service layer directly and just needed a project-scoped entry point instead of resolving a team first. `cargo test` (374/374) clean; live-verified end to end against a scratch server (`caddy` auth mode, personal project): complete a personal task → Activity page shows "completed" with an Undo button → Undo reverses the entry and unchecks the task → a second Undo on the same entry correctly 422s.
  - **Ordering fix, same day:** `reverse_and_reopen` originally called `reverse_entry` (claws back points, marks the entry reversed) *before* attempting the reopen, so undoing a stale entry for a series occurrence that's no longer the series' cursor position would silently reverse the points and mark the entry reversed, then fail to reopen the item (rejected by `item_series::validate_uncompletable`, run internally by the reopen's own `update_project_item` call) — leaving the points gone, the entry unusable (already marked reversed), and the item still complete with its cursor untouched. Fixed by running the same `validate_uncompletable` check as a pre-check *before* `reverse_entry`, so an out-of-order series occurrence is rejected with zero side effects — no points touched, no entry marked reversed. New test: `undo_project_activity_log_entry_rejects_an_out_of_order_series_occurrence_without_reversing_points`. `cargo test` (375/375) clean.