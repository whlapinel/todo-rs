# Rough plan: virtual/computed recurring-event occurrences (RRULE-style)

Status: **rough sketch, deliberately not implementation-ready — not being pursued now.** Captured 2026-08-13 from a planning discussion, for reference if this app's needs outgrow the lighter approach actually chosen (`docs/event-complete-removal-plan.md`, "auto-advance on read"). This doc is intentionally lower-fidelity than a real plan — revisit and flesh out properly before ever implementing.

## When to revisit this

The auto-advance-on-read model (chosen instead, see the sibling doc) only ever materializes **one occurrence at a time** — "what's the next occurrence of this recurring thing," recomputed lazily when stale. That's sufficient for how this app is actually used today. This fuller model becomes worth its cost only if a real need shows up to **browse a range of a recurring series** — e.g. a calendar view that should show "every Monday meeting for the next 3 months," not just the next one — since that's the one thing the lighter model structurally can't do (it only ever has one row per series, representing whichever occurrence is current).

## Core model shift

Today, one `Item` row conflates two things: "the recurrence definition" (pattern, basis) and "the currently-active instance" (a concrete `scheduled_date`, editable fields, an id other things can link to). A real RRULE-style model needs these to be separate concepts:

- **A series**: the recurrence rule + anchor/start date + the event's static fields (name, description, `event_type`, etc.) — this is the thing that has *no* single date, just a rule.
- **An occurrence instance**: a specific date arising from the series, which may or may not be materialized as a real row.

This is exactly the iCalendar (RFC 5545) model: a series (`RRULE`), plus exceptions layered on top — `EXDATE` ("skip this one date entirely") and an overridden instance keyed by `RECURRENCE-ID` ("this one instance has a different time/title/etc. than the rule would produce").

## The materialization problem (the actual hard part)

Every `Item` in this app today is an addressable row with a real id — detail pages, edit links, and `sourceEventId` (a Task pointing at one specific Event) all depend on that. A purely virtual, never-persisted occurrence has no id to hang a detail page, an edit, or a task-link off of. So a genuinely "pure" virtual model doesn't actually work end-to-end for this app — the moment a user wants to *interact* with one occurrence (open it, edit it, link a task to it), it has to become a real row.

The realistic shape is therefore **lazy materialization**: occurrences are computed/virtual for *display* (listing a date range), but the first time someone touches a specific occurrence, a real row gets created for it (carrying the `RECURRENCE-ID`-equivalent — which date in the series this row represents), and from then on that occurrence is "materialized" and behaves like today's items. An occurrence a user explicitly wants to skip becomes an `EXDATE`-equivalent marker on the series rather than ever materializing.

## What would actually need to change

1. **Range-based occurrence generation** in `src/domain/recurrence.rs`. Today's `next_date()` only computes *one* date after a reference point. A real implementation needs something like `occurrences_between(rule, anchor, range_start, range_end) -> Vec<DateTime<Utc>>` — a materially different algorithm (an iterator/generator over the rule within a window), with its own edge cases at month/year/DST boundaries distinct from the single-next-date logic that exists today.

2. **A series/instance split in the domain model** — whether this is a schema change (a new `event_series` concept alongside `items`) or a reinterpretation of the existing `Item::Event` row (with materialized occurrences as separate rows referencing it) is an open design question, not resolved here.

3. **Every current single-`scheduled_date`-value consumer** needs to become range-aware, or explicitly declare "I only want the next occurrence": the project dashboard's list and calendar-month views, the CSV importer/exporter, `Item::is_overdue`, any sort/filter by date. Each of these currently just reads `item.scheduled_date()` as one value.

4. **The calendar month-grid view** specifically would need to expand each series into its occurrences within the visible month for display, most of which would be non-materialized/synthetic (not directly editable — clicking one would trigger materialization first).

5. **`sourceEventId` redesign** — today a Task points at one concrete Event row's id. Under this model it would need to either point at the series and resolve "whichever occurrence is current" at read time (reintroducing a live-computation dependency similar to what auto-advance-on-read already does, just relocated), or allow linking to a specific occurrence that may not be materialized yet (requiring the link itself to trigger materialization).

6. **The lighter mechanism becomes redundant once this lands** — auto-advance-on-read's "one row, kept fresh" model is subsumed by "many correctly-computed occurrences, one of which is always the fresh one." If this is ever built, the auto-advance logic from the sibling doc can be retired.

## Rough sizing

This is closer to "build a recurring-events engine" than any single-session change — it touches the domain model's core identity assumption (one row = one addressable thing), the calendar rendering, the `sourceEventId` feature, and every date-reading surface in the app. Expect real edge-case-prone test surface around occurrence generation (this is exactly the class of bug that's easy to get subtly wrong — the kind of thing `recurrence.rs`'s existing single-`next_date` tests already had to be careful about, multiplied across range generation instead of a single lookup).

No schema, migration, or API-shape decisions are finalized in this document — treat it as a starting point for a real planning session if range-browsing of recurring series ever becomes an actual need, not as something to implement as-is.

## Staged breakdown (added 2026-08-13, still not being pursued now)

The section above deliberately leaves the schema question open ("an open design question, not resolved here"). A *staged* plan can't do that — each stage has to build on a concrete decision made by an earlier one. So this section commits to one working data-model shape, purely so the work can be cut into stages that each compile, test, and commit independently, with context cleared in between. If this is ever picked up for real, re-validate these assumptions first — don't just start executing stage 1 on stale reasoning.

### Working assumptions this staging depends on

- **New tables, not a reinterpretation of `items`.** A new `event_series` table holds the recurrence rule + anchor date + the event's static fields (name, description, `event_type`, `project_id`, ...). A new `event_occurrences` table tracks each date's state: `(series_id, occurrence_date, item_id NULL, is_exdate BOOL)`. This avoids overloading `Item`'s existing shape (see "materialization problem" above) — every other consumer of `items` keeps working unmodified.
- **A materialized occurrence is an ordinary `items` row** (`item_type = Event`), created via `get_or_materialize_occurrence` instead of a normal create call. Its detail page, edit form, `sourceEventId` target, points/activity — everything — works exactly like any Event does today. Only *how it came to exist* differs.
- **Purely additive.** Existing non-series Events are untouched; nothing forces migrating them into a series. A series is an opt-in creation path.
- **Skip semantics deferred to stage 6** — whether skipping an already-materialized occurrence deletes its `items` row or just orphans it is an open call left to that stage, not decided here.

If the actual implementation ends up choosing a different shape (e.g. reinterpreting the `Item::Event` row itself as the series), this staged breakdown doesn't hold — redo the staging, don't patch around it.

### Stages

Each stage is scoped to be independently committable and testable; "clear context" below each heading marks a safe point to compact/reset before starting the next stage.

**Stage 1 — Range-based occurrence generation** (covers change #1 above)
- Add `occurrences_between(rule: &RecurrenceRule, anchor: DateTime<Utc>, range_start: DateTime<Utc>, range_end: DateTime<Utc>) -> Vec<DateTime<Utc>>` to `src/domain/recurrence.rs`.
- Pure function, no schema/DB/consumer changes. Exhaustive unit tests for month/year/DST boundaries, matching the care `next_date`'s existing tests already take.
- Verify: `cargo test` (recurrence module only needs to pass — nothing else is touched).
- Commit: "Add range-based occurrence generation to recurrence.rs".
- Safe to clear context after — nothing later depends on anything but this function's signature.

**Stage 2 — `event_series`/`event_occurrences` storage layer** (covers change #2 above)
- New migration under `src/storage/migrations/` plus matching `CREATE TABLE IF NOT EXISTS` additions in `create_pool()` (per CLAUDE.md's "Adding a DB column"/schema workflow).
- New repo trait, e.g. `EventSeriesRepo` (`#[cfg_attr(test, mockall::automock)]` per existing convention): `create_series`, `get_series`, `list_series_for_project`, `get_occurrence`, `record_materialized_occurrence`, `mark_exdate`, `list_occurrences_between`.
- No handler/service wiring yet — repo-level tests only (mocked in later stages, exercised directly here via a real SQLite pool the way other repo tests do).
- Commit: "Add event_series/event_occurrences storage layer".

**Stage 3 — Materialization service** (covers change #2's runtime half)
- New `src/service/event_series.rs`: `get_or_materialize_occurrence(series_id, date) -> Result<Item, ItemError>` — returns the existing materialized Item if `event_occurrences` already has one for that date, otherwise builds a new Event `Item` from the series' static fields + `date`, persists it via the existing item-creation path, records the mapping, returns it.
- `skip_occurrence(series_id, date) -> Result<(), ItemError>` — sets `is_exdate`; exact behavior toward an already-materialized row deferred to stage 6.
- Still no web_ui/json_api wiring — unit-testable in isolation against mocked repos, same style as existing `service/` tests.
- Commit: "Add occurrence materialization service".
- **Committed** (`4e57677`), per the refined plan at `~/.claude/plans/can-you-tell-me-jiggly-spring.md`: `get_or_materialize_occurrence`/`skip_occurrence` delegate item creation to `project_items::create_project_item` rather than a hand-rolled dispatch of its own — that delegation choice (raised as an open question in an earlier, since-superseded refined-planning pass) is settled and not up for reconsideration. Implemented as specified above, no deviations. 7 new unit tests, full suite (262 tests) and `cargo check` clean.

**Stage 4 — Series CRUD surface** (Smithy + json_api + minimal web UI)
- New Smithy operations for creating/reading/updating a series (rule, anchor, static Event fields) — run `task codegen` and follow CLAUDE.md's "Adding or removing an operation" touch-point checklist in full (handler wiring, MCP tool, CLI subcommand, docs).
- Minimal web UI: a form to create a series and see it listed. Deliberately not enough yet to browse or interact with its occurrences (that's stage 5).
- This is the largest single stage — touches the most files per CLAUDE.md's checklist. Consider sub-splitting further (e.g. Smithy+codegen+json_api as 4a, web UI as 4b) when actually executing, rather than in this rough doc.
- Commit: "Add series CRUD (Smithy + service + minimal web UI)".
- **Stage 4a committed** (2026-08-14), per the refined plan at `~/.claude/plans/joyful-greeting-newell.md`: split exactly as the note above suggested. 4a covers everything except the web UI — `model/src/main/smithy/event_series.smithy` (`CreateEventSeries`/`GetEventSeries`/`UpdateEventSeries`/`ListEventSeriesForProject`, all `/projects/{projectId}/series...`-scoped, no `DeleteEventSeries` — deferred, since deleting a series raises the same materialized-occurrence question stage 6 already defers), `src/json_api/event_series.rs`, `main.rs` wiring (`SqliteEventSeriesRepo` is now actually constructed and layered), four new `EventSeriesRepo::update_series`-backed service passthroughs in `src/service/event_series.rs` (create/get/update/list, all gated on project *membership*, matching `create_project_template`'s auth level rather than `update_project`'s admin gate), MCP tools, `prl series` CLI subcommand, and a new `## Event Series` section in `docs/prl-user-guide.md`. No deviations from the refined plan.

**Stage 4b committed** (2026-08-14), per the refined plan at `~/.claude/plans/frolicking-juggling-wreath.md`: minimal web UI — `src/web_ui/project_event_series/` (list page + standalone `/new` create page, mirroring `project_templates`'s no-per-screen-router module shape and `project_events`'s date/time form-field handling), a new `SidebarSection::EventSeries` nav entry, and the two new routes wired into `build_web_router()`. List-only rows, no detail/edit/delete — no detail page exists yet (delete isn't built at the service/API layer either, per 4a's own deferral). Also fixed a wiring gap 4a left: `SqliteEventSeriesRepo` was constructed and layered into the `api` router but never `web_router` (it was `move`d rather than `.clone()`d into the `api` `ServiceBuilder`), so it was unreachable from any `/web/*` handler until this stage added it to both auth-mode branches' `web_router` layer chains.

**Stage 5 — Range-aware display** (covers changes #3 and #4 above)
- Wire `occurrences_between` + `event_occurrences` into the project dashboard's list/calendar views and the Events month-grid view: for each series overlapping the visible range, render its occurrences — materialized ones link to their real detail page, virtual ones render distinctly (no edit link, a "create/open" affordance).
- Clicking a virtual occurrence calls `get_or_materialize_occurrence` then redirects to the resulting item's detail page.
- Non-series Events (today's model) render unchanged — this stage only adds a second, parallel rendering path for series.
- **Open reconsideration, raised 2026-08-14 during Stage 4b's planning (not resolved here):** the "clicking materializes" design above triggers a real `items` row write from a bare *view* — opening a virtual occurrence's detail page persists it, even if the user changes nothing. The user's preference is that a virtual occurrence should stay virtual until an actual write happens against it (an edit save, a completion, a child add), not merely from being opened. This likely means a virtual occurrence's detail page needs its own read-only rendering path that does *not* call `get_or_materialize_occurrence` at all, deferring that call to whichever action is the first genuine mutation (e.g. the edit form's *submit*, not its *display*). Resolve this properly during this stage's own refined-planning pass rather than deciding it here.
- Commit: "Wire series occurrences into dashboard/calendar views".

**Stage 5 committed** (2026-08-14), per the refined plan at `~/.claude/plans/peaceful-finding-crayon.md`: the "Open reconsideration" above is resolved — the user confirmed materializing on click/view is fine, so no separate read-only virtual-detail rendering path was built; a virtual occurrence's link is a plain `POST` (via `hx-post`) that materializes then `HX-Redirect`s to the real detail page, mirroring `project_templates`' existing "Use" flow precedent exactly. New `event_series::list_virtual_occurrences_for_project_unchecked` (7 new unit tests) composes `list_series_for_project` + `occurrences_between` + `list_occurrences_between` to find occurrence dates with no `event_occurrences` row at all (materialized *or* exdate rows are both excluded by the same check, so a future Stage 6 skip-UI already renders correctly today with zero exdate-specific code here). Wired into all three named surfaces — the project dashboard's list view (`project_dashboard.rs`, a new bounded 90-day default window for presets that leave `(after, before)` open-ended) and calendar view, and the Events month-grid calendar (`project_events/`) — as a second, additive-only data source merged alongside each view's existing real-item query; a materialized occurrence is never double-rendered since it's already covered by the normal item query the moment it's created. New route `POST /projects/:project_id/series/:series_id/occurrences/:occurrence_ts` (Unix-timestamp-seconds path segment, matching `event_occurrences.occurrence_date`'s actual storage type exactly) in `project_event_series/handlers.rs`. The flat Events list view (`/projects/:project_id/events`) was deliberately left untouched, per the doc's own three-surface wording. One known gap flagged, not fixed: if a materialized occurrence's `items` row is later deleted directly, its `event_occurrences` row stays behind and the occurrence just disappears from every view rather than reverting to virtual — folded into Stage 6's already-deferred "what happens to `event_occurrences` on deletion" question rather than addressed here. Full suite (287 tests) and `cargo check` clean; manually smoke-tested end-to-end against a running server (series create → virtual rows in all three surfaces → materialize-on-click → confirmed no duplication/loss after re-fetch).

**Stage 6 — Skip (EXDATE) UI**
- Add a "skip this occurrence" action wherever an occurrence (virtual or materialized) is shown; resolve the deferred skip-after-materialize behavior from stage 3 here.
- Commit: "Add skip-occurrence (EXDATE) UI".

**Stage 6 committed** (2026-08-14), following its own refined-planning pass via `AskUserQuestion` (this stage's one-paragraph sketch left both halves of "resolve the deferred skip-after-materialize behavior" genuinely open, so — per this doc's own closing note — it wasn't implemented off the sketch directly): two questions were put to the user before writing any code.
  - **Skip-after-materialize semantics**: confirmed as "delete the item, then mark exdate" — skipping a materialized occurrence now deletes its `items` row and marks the occurrence exdate, rather than the stage-3 placeholder of leaving the item orphaned-but-untouched. `EventSeriesRepo::mark_exdate`'s SQL now unconditionally clears `item_id` on the upsert (previously it preserved it) — correct now that nothing calls `mark_exdate` against a still-live item.
  - **Skip UI scope**: confirmed as "virtual occurrences only" — the new Skip button is wired onto virtual occurrences in the three Stage 5 surfaces (dashboard list, dashboard calendar, events calendar), not onto materialized ones. A materialized occurrence carries no visible marker distinguishing it from an ordinary Event in those views, so giving it its own dedicated Skip button would have meant a new reverse lookup surfaced into all three; instead, a materialized occurrence's only "skip" affordance is deleting its item via the item's own existing Delete action (its detail page, wherever that already lives).

  That second decision is what connects the two: since "delete the item" is the materialized occurrence's skip action, `service::project_items::delete_project_item` — the one shared delete path every caller (web UI, CLI, MCP) already routes through — now takes an `EventSeriesRepo` and, after deleting, calls a new `service::event_series::unlink_deleted_item_occurrence(event_series, item_id)`: a reverse lookup (new `EventSeriesRepo::find_occurrence_by_item_id`, backed by a new `idx_event_occurrences_item_id` index — migration 15, `AddEventOccurrencesItemIdIndex`) that finds and marks exdate on the occurrence if the deleted item came from a series, and is a cheap no-op otherwise. This also fixes the Stage 5 "known gap": a materialized occurrence's item deleted through *any* existing delete path (not just an intentional "skip") now correctly reverts its slot to virtual instead of leaving a stale `item_id` behind.

  Threading the new `EventSeriesRepo` parameter through `delete_project_item` touched all six of its callers (`json_api::project_items::delete_project_item`, and the web UI's task/event/simple-list/template/template-child delete handlers) — mechanical signature changes, no behavior change for any of them beyond the new reverse-lookup no-op. New Skip button targets a sibling route, `POST .../series/:series_id/occurrences/:occurrence_ts/skip` (`skip_project_event_series_occurrence_form`, alongside the existing materialize route), calling `service::event_series::skip_occurrence` — unchanged from Stage 5 aside from its doc comment, since the UI only ever invokes it against genuinely virtual dates. Unlike materialize's `HX-Redirect`, skip's handler returns an empty 200 body; each surface's Skip button targets its own row/entry element (`hx-target` + `hx-swap="outerHTML"`) so the occurrence is removed from view in place, mirroring `project_dashboard/row.html`'s existing item-delete-button convention exactly. 14 new unit tests (storage-layer `find_occurrence_by_item_id` + `mark_exdate`'s clearing behavior, service-layer `unlink_deleted_item_occurrence`, a `delete_project_item` wiring test) and a new migration idempotency test; full suite (285 tests) and `cargo build` clean.

## Scope change (2026-08-14): generalizing series to Tasks, not just Events

Raised at the start of Stage 7 planning: rather than implementing Stage 7 narrowly as originally scoped (`sourceEventId` → series resolution only), the user asked whether Tasks should get their own series concept too — Tasks still recur via the old single-row `Item::next_recurrence`/`clone_children`/`sync_offset_children` mechanism (CLAUDE.md's Recurrence section) while Events have `event_series` (Stages 1–6, committed). Confirmed via `AskUserQuestion` before drafting anything below, this **replaces** the original Stage 7/8 sketches (kept just below, unmodified, for history — treat them as superseded, not current).

Three decisions, resolved before staging:

1. **Materialization trigger is per-series, both kinds available.** A series carries `item_type` (`TASK` | `EVENT`). Event-typed series keep today's range-browse/click-to-materialize UX (Stage 5/6) unchanged. Task-typed series get *both*: the same range-browse display (an upcoming task series shows future dates in a list, like Events do today), *and* a new completion-driven auto-advance — completing the current materialized occurrence materializes the next one automatically. Events never had completion-driven advance under the series model (only under the old single-row mechanism); this is genuinely new behavior, built in Stage 9 below.
2. **The old single-row recurrence mechanism is retired for both Task and Event**, once series covers it — supersedes the original Stage 8's "Tasks keep the old mechanism regardless" call, which predates this scope change. Retirement itself is Stage 10, last, after everything it depends on exists and is proven.
3. **Rename, not reuse.** `event_series`/`event_occurrences` tables, `EventSeriesRepo`, the `*EventSeries` Smithy operations, `src/service/event_series.rs`, `src/web_ui/project_event_series/`, and the MCP `*_event_series` tools all get renamed to the generic `item_series`/`ItemSeriesRepo`/`*ItemSeries`/etc. — chosen over bolting an `item_type` column onto the Event-named surface, since the entire point is that a Task-typed row would otherwise live in something called "event_series," the same class of stale-name confusion CLAUDE.md flags elsewhere (e.g. `TeamItem`'s retired Smithy surface vs. its still-live service functions). `prl series` (the CLI command) and `prl-user-guide.md`'s "Event Series" section (→ "Item Series") don't need a command-name rename — `series` was already generic; only its flags/help text change.

### Additional working assumptions for this redesign

- **Existing `event_series`/`event_occurrences` rows are live production data** (Stages 1–6 are committed and deployed) — the rename must carry data forward via a copy, not just a schema change. Old tables are left in place afterward, unread, rather than dropped — matches this codebase's existing precedent of not force-dropping superseded columns (`items.user_id` survived the Project abstraction; see Storage Layer section of CLAUDE.md).
- **`get_or_materialize_occurrence` creates a bare item today** — verified in `src/service/event_series.rs`: no child/`parent_item_id` handling exists at all, unlike the old mechanism's `clone_children`, which recomputes and re-parents an entire subtree via `due_offset_days` on every recurrence. Whether Task series need their own "template children" concept (mirroring `copy_template_children`) to replace that is an **open question**, deliberately not resolved here — it's Stage 9's own refined-planning pass to make, flagged explicitly so it isn't silently dropped.
- **`ItemType` has four variants**, not three — `TASK`/`EVENT`/`TEMPLATE`/`SIMPLE` (verified directly against `model/src/main/smithy/item.smithy`; CLAUDE.md's own prose describing it as three variants is stale and should be corrected separately). A series' `item_type` is restricted to `TASK`/`EVENT` only, mirroring how `event_type` is already restricted to those same two types in `Item::validate()`.

### Stages (supersede original Stage 7/8 below)

**Stage 7a — Rename schema + repo, migrate data**
- New migration: `CREATE TABLE item_series` / `item_occurrences`, with `item_type TEXT NOT NULL DEFAULT 'EVENT'` present from the start (the original `event_series` migration had no such column) — then `INSERT INTO item_series SELECT ..., 'EVENT' FROM event_series` and the equivalent for occurrences, column-for-column.
- Rename `EventSeriesRepo` → `ItemSeriesRepo` (all methods); update `create_pool()`'s baseline `CREATE TABLE IF NOT EXISTS` to the new names+column so a fresh DB never touches the migration.
- Repo-level tests only — no handler wiring changes yet.
- Commit: "Rename event_series to item_series, add item_type column, migrate data".

**Stage 7a committed** (2026-08-14): implemented as specified above, with the mechanical propagation this doc's own bullet list didn't spell out but the rename necessarily entails — every existing call site that referenced the old trait/domain-type names (`src/json_api/event_series.rs`, `src/json_api/project_items.rs`, `src/service/event_series.rs`, `src/service/project_items.rs`, `src/main.rs`, `src/web_ui/project_dashboard.rs`, `src/web_ui/project_event_series/{handlers,templates}.rs`, `src/web_ui/project_{events,simple_lists,tasks,templates}/handlers.rs`) was updated to `ItemSeriesRepo`/`ItemSeries`/`ItemOccurrence`/`SqliteItemSeriesRepo`, since the old names no longer exist to reference — otherwise nothing would compile. Left deliberately untouched, per this stage's own scope and stage 7b/7c's: the `event_series`/`project_event_series` *module paths* (json_api/service/web_ui), `CreateEventSeriesParams`/`UpdateEventSeriesParams`, every Smithy-generated type (`model::EventSeriesSummary`, `input`/`output`/`error::*EventSeries*`), and `SidebarSection::EventSeries`.

Two calls not spelled out in the bullets above: `domain::ItemSeries.item_type` is typed as the existing `ItemKind` enum (not a raw `String`) since `ItemKind` already has the `FromStr`/`Display`/`as_str` round-trip `Item`'s own `item_type` column uses (`storage/sqlite/mod.rs`'s `row_to_item`) — the repo layer parses/writes it the same way. `create_series`/`update_series` (`src/service/event_series.rs`) — unchanged in the params they accept, since `CreateEventSeriesParams`/`UpdateEventSeriesParams` gain no `item_type` field until 7b — hardcode `ItemKind::Event` on create (the only kind creatable before 7b) and carry `current.item_type` forward unmodified on update (there being no field in `UpdateEventSeriesParams` to source a change from). The old `event_series`/`event_occurrences` tables are left in place and unread, per the doc's own working assumption; `AddItemSeries` (migration version 16) copies every row across with `item_type = 'EVENT'`, guarded idempotent via `WHERE id NOT IN (...)`. `cargo build`/`cargo test` clean (289 tests, 3 pre-existing migration-count assertions bumped 15→16).

**Stage 7b — Smithy + json_api + service + MCP + CLI rename**
- Every `EventSeries` Smithy shape/operation → `ItemSeries`, following CLAUDE.md's "Renaming an error type" touch-point table as the closest precedent; add `itemType` to Create/Update/Get/List shapes. `task codegen`.
- `src/json_api/event_series.rs` → `item_series.rs`, `src/service/event_series.rs` → `item_series.rs` (rename file + every fn/type), `main.rs` wiring updated.
- `get_or_materialize_occurrence` stops hardcoding `item_type: Some(ItemKind::Event)`, uses `series.item_type` instead. `create_item_series` rejects `item_type` outside `TASK`/`EVENT` via `ItemError::Invalid` — same rejection pattern as the recurrence+`parentItemId` check.
- MCP tools `*_event_series` → `*_item_series`, `itemType` added as a required create/update parameter (no default — forces every caller to be explicit, since this is a behavior change, not a cosmetic one).
- `prl series create`/`update` get a required `--item-type <task|event>` flag, client-side-rejected if missing (same posture as `--project`, Stage C3).
- Commit: "Rename EventSeries API surface to ItemSeries, add itemType".

**Stage 7b committed** (2026-08-14), via its own refined-planning pass (`AskUserQuestion`-free — the plan mode session confirmed the sketch's own three cross-cutting calls were sufficient, resolving only one remaining implementation detail: `UpdateItemSeriesParams` gains `item_type` too and joins the rest of update's already-full-replace fields — a series's kind becomes explicitly re-settable on every update call, not immutable as `update_series` treated it pre-7b). Implemented as specified above, no deviations: `model/src/main/smithy/event_series.smithy` renamed to `item_series.smithy` (every shape/operation `EventSeries`→`ItemSeries`, `itemType: ItemType` added as `@required` on `ItemSeriesSummary`/`CreateItemSeries`/`GetItemSeries`/`UpdateItemSeries`'s in-/output shapes, reusing `item.smithy`'s existing closed enum), `service.smithy`'s operations list updated, `task codegen` clean. `src/json_api/event_series.rs`→`item_series.rs` and `src/service/event_series.rs`→`item_series.rs` (fn/type renames throughout, `to_domain_item_type`/`to_sdk_item_type` from `json_api::mod` wired in); `service::item_series::create_series`/`update_series` both gained a `validate_series_item_type` check (`ItemError::Invalid` for anything but `TASK`/`EVENT`, mirroring the `recurrence`+`parentItemId` precedent in `service::items`); `get_or_materialize_occurrence` now reads `series.item_type` instead of hardcoding `Event`. `main.rs`'s Smithy-facing imports/builder registrations updated; the web UI's own `use crate::service::event_series::...` import paths (`project_dashboard.rs`, `project_events/{handlers,mod}.rs`, `project_event_series/handlers.rs`, plus a doc-comment reference in `storage/sqlite/mod.rs`) were mechanically repointed at the new module path to keep compiling, without renaming the web UI itself (still Stage 7c) — `project_event_series/handlers.rs`'s one `CreateItemSeriesParams` construction site hardcodes `item_type: ItemKind::Event`, matching the web UI's current Event-only behavior until 7c adds a real selector. MCP's 4 tools renamed `*_event_series`→`*_item_series` with `itemType` (enum `["TASK","EVENT"]`) added to each tool's `required` list, written unconditionally into the request body (no default, unlike the *optional* `itemType` pattern on `create_item`/`update_item`). `prl series create`/`update` gained a required `--item-type <task|event>` flag (`Option<String>` clap field + manual `let Some(...) else { eprintln!(...); exit(1) }`, mirroring `--project`'s posture rather than `items add`'s optional `--item-type`), plus a 2-variant `parse_series_item_type_flag` helper (only `TASK`/`EVENT` are valid here, so `items.rs`'s own 3-variant helper wasn't reused). 5 new service-layer tests (Task-typed series create/materialize, Template/Simple rejection on both create and update); full suite (294 tests), `cargo build`, MCP `npm run build`, and `todo-cli` build all clean. Manually smoke-tested end-to-end against a running local server: created Task- and Event-typed series, confirmed a missing `itemType` 400s with a Smithy-level constraint-violation message, confirmed a `TEMPLATE` `itemType` 400s with the service-layer message, confirmed GET/LIST round-trip `itemType`, confirmed UPDATE can flip a series's type, confirmed the web UI list page still renders unchanged.

**Stage 7c — Web UI rename**
- `src/web_ui/project_event_series/` → `project_item_series/`; list page gets an item-type badge/column, the standalone `/new` create page gets an item-type selector.
- `SidebarSection::EventSeries` → `SidebarSection::ItemSeries` (or split into two sidebar entries — decide during this stage's own refined pass).
- `docs/prl-user-guide.md`'s "Event Series" section renamed "Item Series", documents `--item-type`.
- Commit: "Rename Event Series web UI to Item Series".

**Stage 7c committed** (2026-08-14): implemented directly (no `AskUserQuestion` pass — every open item below was either mechanical or resolved a concrete bug the sketch's own selector would otherwise introduce, not a genuine design fork). `src/web_ui/project_event_series/` → `project_item_series/` via `git mv`, every fn/type renamed (`ProjectEventSeriesRow`→`ProjectItemSeriesRow`, `ProjectEventSeriesListPageTemplate`→`ProjectItemSeriesListPageTemplate`, `NewProjectEventSeriesPageTemplate`→`NewProjectItemSeriesPageTemplate`, `CreateEventSeriesForm`→`CreateItemSeriesForm`, all 5 handler fns `*_event_series_*`→`*_item_series_*`); `templates/project_event_series/`→`project_item_series/` alongside it; `web_ui::mod`'s `pub mod` line and `main.rs`'s import/route wiring updated to match. `SidebarSection::EventSeries`→`ItemSeries`: kept as one sidebar entry (not split), since the list page already disambiguates per-row via the new badge and a second nav entry would need its own filtered query with no clear use case yet. `docs/prl-user-guide.md`'s section was already titled "Item Series" with `--item-type` documented (done ahead of schedule, apparently during 7b's CLI work) — verified via grep, no doc changes needed here.

  Two gaps surfaced by adding the item-type selector, fixed as part of this stage rather than deferred, since the selector itself is what makes them reachable through the web UI for the first time:
  - `service::item_series::create_series`/`update_series` gained `validate_series_event_type` (`ItemError::Invalid` unless `item_type == Event`, mirroring `validate_series_item_type`'s precedent) — without it, a Task series with `event_type` set would silently drop the value forever at every materialization (`domain::item::build_item_type` has no `event_type` slot on `ItemType::Task` at all, so nothing errors, it just vanishes). Actually latent since Stage 7b's CLI/MCP `--item-type`/`itemType` support, not introduced by this stage. 3 new unit tests.
  - `materialize_project_item_series_occurrence_form`'s redirect was hardcoded to `.../events/:id`, wrong for a materialized Task. Rather than hand-rolling a second kind-to-URL match, `project_dashboard::detail_url` (already doing exactly this dispatch for real items) was made `pub(crate)` and reused. Also latent since 7b — reachable today via `prl series create --item-type task`, surfaced now because the web UI can finally create one itself. Verified end-to-end against a running local server: created a Task- and an Event-typed series, confirmed list-page badges (gray "Task" / indigo "Event"), confirmed the Task+eventType combo 422s with the new message, confirmed materializing a Task occurrence redirects to `/tasks/:id`.

  New page markup: the `/new` form's Kind radio (Task/Event, Task checked by default) mirrors `macros::kind_fields`' existing radio styling but isn't built on that macro — `kind_fields` also offers Simple and its own toggle script only ever hides `.scheduling-fields`, not the event-type field, so reusing it would have required fighting its behavior rather than using it. A small inline script (same `document.currentScript.closest("form")` pattern `kind_fields` itself uses) disables+hides the event-type field when Task is selected, so it isn't submitted at all in that case. `cargo build`/`cargo test` clean (297 tests, +3 over 7b's 294).

**Stage 8 — Task-series range-browse display**
- Mirror Stage 5's wiring onto the dashboard (list + calendar — already generic infra, needs only a type-awareness fix) and the Tasks month-grid calendar (`project_tasks/`, genuinely new wiring) — confirmed via `AskUserQuestion` during this stage's own refined-planning pass that the flat `/tasks` list is deliberately excluded, mirroring how Stage 5 left the flat Events list untouched (neither has a date-range concept to browse within).
- **Past-date clamp, resolved during this stage's planning pass**: an unmaterialized Task occurrence whose date has already passed is a different thing than a missed virtual Event occurrence — Events have no completion concept, so nothing is ever really "missed"; a recurring Task genuinely can be. Deciding what a missed/backlog Task occurrence *means* (does completing it late catch up one-by-one? does it need its own "overdue" treatment?) is real design work that belongs to Stage 9, not this one — so Stage 8 sidesteps it rather than half-answering it: both range-browse surfaces filter out any Task-typed virtual occurrence with `occurrence_date < now`, full stop. Event-typed virtual occurrences are left exactly as they behave today (unclamped) — out of scope, already-shipped. This means a Task series that's fallen behind simply shows nothing until Stage 9 gives it a real backlog concept, rather than Stage 8 inventing a throwaway one.
- Materializing via the same (now type-agnostic) `POST .../series/:series_id/occurrences/:occurrence_ts` route — confirmed to need no changes (`project_dashboard::detail_url` already dispatches `Task`→`/tasks/:id` since Stage 7c).
- Commit: "Wire Task-series occurrences into dashboard/task-calendar views".

**Stage 9 — Task-series completion-driven auto-advance**
- Resolve the deferred template-children question from the working assumptions above first, via this stage's own refined-planning pass — not decided in this doc.
- Completing a materialized `TASK`-typed occurrence calls `recurrence::next_date` (already exists — a single-next-date lookup, no need for `occurrences_between` here) to find the next date, then `get_or_materialize_occurrence` to create it, taking over `Item::next_recurrence`'s role for series-backed tasks specifically (non-series tasks untouched until Stage 10).
- Skip (EXDATE) UI from Stage 6 already works unchanged for Task-typed series — verified `skip_occurrence` and the Skip button are item-type-agnostic today.
- **Missed/backlog-occurrence design, resolved via discussion during Stage 8's planning (2026-08-14), not yet implemented — this is what Stage 9 should build**: deriving "the current occurrence" by scanning `item_occurrences` back to the series anchor on every read doesn't scale (unbounded cost for an old, faithfully-completed series). Instead, add an explicit `cursor_date` field to `item_series`. "Current" occurrence for a Task-typed series = `next_date(cursor_date)`. Both actions that settle the current occurrence advance the cursor by exactly *one step*, symmetrically — they differ only in what gets written, not in how far the cursor moves:
  - **Complete** the current occurrence: materialize it (`get_or_materialize_occurrence`) and mark it complete, then `cursor_date = current_date`. The new current (`next_date(cursor_date)`) may still be in the past — completing one step at a time is expected; the user works through backlog occurrence-by-occurrence, not all at once.
  - **Skip** the current occurrence: write an exdate row for that date (no materialization — same shape as Stage 6's existing virtual-skip), then `cursor_date = current_date`, identical cursor advance to Complete.
  - A later, separate convenience feature — not part of Stage 9's initial scope — can add a bulk "settle everything through today in one action" on top of this same one-step primitive (loop it, or a dedicated endpoint); the base Complete/Skip actions themselves stay single-step.
  - This also directly resolves Stage 8's past-date clamp: once this lands, the "current" backlog occurrence (if any) is a single, well-defined thing a Task series can surface distinctly from its future range-browse occurrences, rather than Stage 8's blanket "just hide anything in the past."
- Commit: "Add completion-driven auto-advance for Task-typed series".

**Stage 9 committed** (2026-08-14), via its own refined-planning pass (`AskUserQuestion`, four questions, all resolved with the recommended option) before any code was written, per this doc's own closing convention:
  - **Template children**: deferred, not built. A materialized Task-series occurrence stays a bare item, exactly like an Event-series occurrence already is — matches existing precedent, keeps this stage scoped.
  - **Complete trigger**: hooks into *normal* item completion, not a dedicated "Complete current occurrence" button. Any surface that completes an already-materialized Task-series item — its detail-page checkbox, the dashboard row toggle (`toggle_project_dashboard_item_complete`), the CLI, MCP — advances the cursor automatically. A materialized occurrence still has to exist first, via the same range-browse materialize-on-click flow Events already use (Stage 5).
  - **Out-of-order completion**: the cursor jumps forward to whatever occurrence's date was just completed/skipped, full stop — implemented as an unconditional forward-only `MAX` in `ItemSeriesRepo::advance_cursor`, so completing occurrences out of order can never move the cursor backward, but also never "remembers" older backlog once something later has been settled.
  - **UI surface**: no new widget. The three existing range-browse surfaces (dashboard list, dashboard calendar, Tasks month-grid) now render an `is_current` badge ("Current" vs "Planned", or a ring on the calendar's type-symbol dot) on whichever virtual occurrence is the series' current one.

  One contradiction in this sketch surfaced during grounding, fixed before writing any Stage 9 code: `recurrence::next_date` (what the two bullets above literally named) unconditionally clamps its result to be after `Utc::now()` — it can *never* return a past date, which would have silently defeated the entire backlog feature the moment a series fell behind. Fixed by adding `recurrence::advance_once(rule, reference, tz_offset_minutes)` — a thin, non-clamping wrapper around the same private `advance()` step `next_date` itself calls — and using that instead. `next_date` itself is untouched; the legacy `Item::next_recurrence` mechanism still depends on its clamping behavior until Stage 10.

  Implemented otherwise as specified: `item_series.cursor_date: Option<DateTime<Utc>>` (new nullable column, migration 17 `AddItemSeriesCursorDate`, guarded by `column_exists` per the standard pattern — `create_pool()`'s baseline updated too). `ItemSeriesRepo::advance_cursor(series_id, occurrence_date)` does the forward-only `MAX(COALESCE(cursor_date, ?), ?)` update atomically, no read-then-write race. `service::item_series::current_occurrence_date(series, rule, tz_offset)` is the pure "current" derivation (`cursor_date.map(|c| advance_once(rule, c, tz)).unwrap_or(series.anchor_date)`) — a fresh series' current occurrence is its own anchor, not one step past it. `record_task_completion(event_series, item_id)` is the new completion-side hook, mirroring `unlink_deleted_item_occurrence`'s shape exactly: a reverse lookup via `find_occurrence_by_item_id`, a cheap no-op for the overwhelmingly common non-series case, gated to Task-typed series only. `skip_occurrence` gained the identical cursor advance for Task series (Event series untouched — no completion/cursor concept). Both hooks are deliberately *not* gated on detecting an actual incomplete→complete transition — `advance_cursor`'s own idempotent forward-only max already makes a redundant call harmless, so `project_items::update_project_item` (which gained a new `event_series: &Arc<dyn ItemSeriesRepo>` parameter, threaded through all ten of its callers — json_api's handler plus nine web_ui form handlers across `project_tasks`, `project_dashboard`, `project_templates`, `project_simple_lists`, `project_events` — the same mechanical propagation Stage 6 did for `delete_project_item`'s six callers, just larger) just calls the hook whenever `params.complete` is true, after the dispatch succeeds.

  `list_virtual_occurrences_for_project_unchecked` absorbed Stage 8's past-date clamp (previously duplicated across three separate `web_ui` call sites) and now computes `is_current` itself, since only it has the series' `cursor_date` needed to compute the exemption: a Task-typed virtual occurrence in the past is dropped *unless* it equals `current_occurrence_date` — the one backlogged occurrence a Task series is allowed to surface. `VirtualOccurrence` gained the `is_current: bool` field; the three `web_ui` call sites (`project_dashboard.rs` list + calendar views, `project_tasks/handlers.rs`'s calendar) had their now-redundant local `occurrence_date >= now` filters deleted.

  27 new/changed tests (recurrence's `advance_once`, the repo-layer `advance_cursor` forward-only/idempotency/not-found cases, `current_occurrence_date`'s anchor-vs-cursor derivation, `record_task_completion`'s Task/Event/non-series cases, `skip_occurrence`'s new cursor advance, `list_virtual_occurrences_for_project_unchecked`'s backlog-exemption and future-dates-unclamped cases, and `update_project_item`'s new completion-hook wiring test) plus three pre-existing migration-count assertions bumped 16→17; full suite (315 tests), `cargo build`, and `cargo check` all clean. Not manually smoke-tested against a running server this session — recommend doing so before relying on this in production, per this doc's own precedent for prior stages.

**Stage 10 — Migrate existing single-row recurring items onto item_series, retire the old mechanism**
- Highest-risk stage in this redesign — touches live production data (recurring Tasks/Events created before this redesign existed) and retires code (`Item::next_recurrence`, `clone_children`, `sync_offset_children`, the `recurrence`/`recurrence_basis`/`due_offset_days` special-casing in `update_item`/`update_team_item`) that CLAUDE.md's Recurrence/Events/Completion-transition-guards sections describe as load-bearing across CSV import, the CLI, and MCP.
- One-time data migration: for every existing top-level `Item` with `recurrence` set that isn't already series-backed, create an `item_series` row from its `recurrence`/`recurrence_basis`/anchor fields plus an `item_occurrences` row linking its current `item_id` as the materialized occurrence for its current date, so it keeps working under the new mechanism.
- Own refined-planning pass to decide: whether existing recurring items' child subtrees migrate cleanly given Stage 9's template-children design, and whether the CSV import format's `recurrence`/`recurrenceBasis` columns need to change, or can keep writing the old fields with a lazy per-item migration on next completion instead of one big-bang pass.
- Only after this lands and is verified stable in production: delete the old mechanism's code paths.
- Commit: "Migrate legacy recurring items onto item_series; retire single-row recurrence mechanism".

**Stage 10 planning notes (2026-08-14), not yet resolved with the user — read before starting this stage.** A refined-planning session got as far as research (code-reading, no code written) and surfaced that "migrate everything and retire the old mechanism" isn't a single clean move: three real design gaps exist between the legacy per-item mechanism and `item_series`, each with production-data consequences of its own. The user asked to pause here rather than force all three decisions in one sitting — this section exists so a future session doesn't have to re-derive them.

1. **`COMPLETION_DATE` basis has no `item_series` equivalent — resolved 2026-08-14, design confirmed with the user, not yet implemented.** Legacy `recurrence_basis: "COMPLETION_DATE"` measures the next occurrence from *actual completion time* (`Item::next_recurrence`, `src/domain/item.rs`, uses `Utc::now()`). `item_series`'s `cursor_date`/`current_occurrence_date` model (`src/service/item_series.rs`) measures from the *previous scheduled* occurrence via `recurrence::advance_once` — a fixed-schedule engine, not a relative-to-completion one by default. Resolved design, reasoned through with the user rather than assumed:
   - Only "every N hours/days/weeks/months/years"-style patterns are eligible for completion basis (not e.g. "every month on the 3rd" or a fixed weekday) — narrower than schedule-basis series need to support.
   - `record_task_completion` (`item_series.rs:134`) writes `Utc::now()` into `advance_cursor` instead of the occurrence's nominal date, when the series is completion-basis. `current_occurrence_date`'s own formula (`advance_once(rule, cursor_date, tz)`) is unchanged — it doesn't need to know *why* `cursor_date` has whatever value it has, so this correctly reproduces "N units after actual completion" with no changes to that function.
   - Range-browse display (`list_virtual_occurrences_for_project_unchecked`) still shows a full predicted future list for completion-basis series, on the reasonable assumption the assignee completes roughly on schedule — but rooted at `current_occurrence_date`, not `series.anchor_date`. Rooting at the original anchor would silently drift wrong after the first off-schedule completion (the fixed anchor-rooted sequence and the real cursor-chained trajectory permanently diverge); rooting at `current_occurrence_date` self-corrects every render, since nothing here is cached — it's recomputed fresh each call, so a stale prediction just gets silently replaced next time the cursor moves. When `cursor_date` is `None` this is identical to `anchor_date`, so a fresh series behaves the same as before.
   - This design requires the cross-cutting "current-occurrence-only" restriction below to stay coherent (a speculative future completion would otherwise jump the cursor to a date the predicted list never actually promised).
   - Deferred, explicitly out of scope for now: letting the user optionally supply a completion date/time (constrained to the past) instead of always using `Utc::now()`. Noted for a future pass, not blocking this design.
   - Still needs: a `recurrence_basis`-equivalent field on `ItemSeries`/`item_series` (schema addition — not yet designed in detail) restricted to `SCHEDULE`/`COMPLETION`-style values, not the full `DUE_DATE`/`SCHEDULED_DATE`/`COMPLETION_DATE` set `Item` has, since a series has no `due_date` vs `scheduled_date` distinction to preserve in the first place (`get_or_materialize_occurrence` always writes `scheduled_date`).
   - **Committed 2026-08-15**: implemented as designed above, with the field named `basis: Option<String>` on `ItemSeries`/`item_series` (nullable `TEXT`, migration version 18, `AddItemSeriesBasis`) — a plain, unvalidated-by-Smithy string following the `Item::recurrence_basis` precedent (`ItemType` is the documented exception, not the norm), not a second closed enum. `is_completion_basis(series)` (`src/service/item_series.rs`) checks `basis.as_deref() == Some("COMPLETION")`. `validate_series_basis`, called from both `create_series`/`update_series`, rejects `COMPLETION` on a non-`Task` series and on any recurrence pattern other than `RecurrenceUnit::Days/Weeks/Months/Years` (i.e. not `MonthlyDay`/`WeeklyDay`) — matches the "every N days/weeks/months/years" restriction above exactly (the doc's "hours" mention above was aspirational; `RecurrenceRule` has no hourly unit, so the implemented restriction is days/weeks/months/years only). A shared `cursor_value_for_settlement(series, occurrence_date)` helper (returns `Utc::now()` for a completion-basis series, `occurrence_date` otherwise) is called from both `record_task_completion` and `skip_occurrence`'s cursor-advance, keeping the two symmetric per Stage 9's existing design. `list_virtual_occurrences_for_project_unchecked` now computes `current_date` before building `candidates` (previously computed after) and roots `occurrences_between` at `current_date` instead of `series.anchor_date` for a completion-basis Task series, exactly as designed. Full Smithy/json_api/web-UI-create-form/CLI/MCP touch-point checklist completed (`basis` is an optional field on `CreateItemSeries`/`UpdateItemSeries`/`GetItemSeries`/`ItemSeriesSummary`, `prl series create|update --basis schedule|completion`, MCP `create_item_series`/`update_item_series` gained a `basis` enum property) — no web-UI *edit* form exists for series at all (confirmed by reading `src/main.rs`'s route table; `UpdateItemSeries` is API/CLI/MCP-only), so only the create form gained a basis checkbox, gated visible by the existing `itemType` toggle script. 331 tests (9 new), full suite green; `cargo build`/`check`, `task codegen`, and `mcp-server`'s `npm run build` all clean. One implementation-time discovery not anticipated in planning: the `rrule` crate normalizes `DTSTART` to whole-second precision internally, so a sub-second `Utc::now()`-derived test fixture broke boundary-inclusion assertions in `occurrences_between` — unrelated to production code (which never round-trips sub-second precision through SQLite anyway, see `to_secs`/`from_secs`), fixed by truncating the test's fixture dates to whole seconds, matching every other date fixture already in that test file. Manually smoke-tested end-to-end against a throwaway local server/DB (existing dev server on 3001 left untouched): confirmed `COMPLETION` basis rejected on an `EVENT` series and on an ineligible pattern ("every monday"), confirmed a valid `COMPLETION`-basis "every 2 days" Task series round-trips via `GetItemSeries`, and confirmed completing its materialized anchor occurrence advanced `cursor_date` to the actual completion instant (not `anchor_date + 2 days`) and that the dashboard's next-"Current" virtual-occurrence link timestamp equaled `cursor_date + 2 days` exactly, not an anchor-rooted date.

**Cross-cutting decision, resolved 2026-08-14 alongside the above: Complete and Skip only ever apply to a series' current occurrence, never a future one — for every Task series, regardless of basis.** "Current" is purely `current_occurrence_date`'s cursor-derived value, not a comparison against wall-clock time — it can legitimately be in the future (a well-maintained series is normally ahead of today), the past (backlog), or today, and settling it early or late is normal, expected use. What's disallowed is settling anything *beyond* current — you can't jump ahead to a later occurrence while the current one is still unsettled. This is a deliberate reversal of already-shipped Stage 9 behavior (`652724c`), whose own `AskUserQuestion` chose unrestricted out-of-order completion with a forward-jumping cursor — that decision is being revisited, not layered on top of.

**Stage 10a committed (2026-08-15): the cross-cutting decision above, implemented on its own, ahead of the other Stage 10 gaps** — per this doc's own per-sub-stage discipline (every prior sub-stage of this redesign got its own refined-planning pass immediately before its own commit), and because the other three gaps' designs (completion basis, child carry-forward) explicitly depend on settlement only ever happening one occurrence at a time, in order. Implemented as sketched in the paragraph above, with the rejection point clarified during planning: `record_task_completion` (`src/service/item_series.rs`) only ever runs *after* `update_project_item` has already persisted a completion, so it can't be the thing that rejects an invalid one — a new pure helper, `require_current_occurrence(series, occurrence_date, tz_offset_minutes)`, and a new public pre-persistence entry point, `validate_completable(event_series, item_id, tz_offset_minutes)` (same "cheap no-op for the common case" shape as `record_task_completion`/`unlink_deleted_item_occurrence`), were added instead. `project_items::update_project_item` calls `validate_completable` before its `match project.team_id { ... }` dispatch, gated on `params.complete` the same unconditional way `record_task_completion` already was — so the two do a small amount of redundant `find_occurrence_by_item_id`/`get_series` lookup work on every completion, deliberately accepted rather than restructured, matching this codebase's existing tolerance for cheap redundant no-op lookups. `skip_occurrence` gained a `tz_offset_minutes: i32` parameter and calls the same `require_current_occurrence` check immediately after fetching `series`, before either `mark_exdate` or `advance_cursor` runs, so a rejected skip mutates nothing; its one web UI caller (`skip_project_item_series_occurrence_form`) gained a `TzOffset` extractor to supply it. `record_task_completion` itself is unchanged — still just advances the cursor — its doc comment was updated to note `advance_cursor`'s forward-only `MAX` is now a pure idempotency guard rather than something resolving genuine out-of-order jumps, since that possibility no longer exists. No Stage 9 tests needed deleting — none asserted the old forward-jump behavior by name, only that *some* cursor advance happened, which is still true. 11 new/changed unit tests (6 new: `require_current_occurrence`/`validate_completable` rejecting a non-current Task occurrence, allowing the current one, always allowing Event-typed series, no-op for a non-series item; `skip_occurrence` rejecting a non-current occurrence without mutating; 4 existing `skip_occurrence` call sites updated for the new parameter; 1 existing `update_project_item` test's mock expectation bumped from `.times(1)` to `.times(2)` on `find_occurrence_by_item_id`, matching the new double-lookup). Full suite (322 tests), `cargo build`, and `cargo check` all clean. Manually smoke-tested end-to-end against a running local server on a throwaway DB/port (left the existing dev server on 3001 untouched): created a Task-typed series ("every 1 days"), materialized its 2nd occurrence out of order and confirmed completing it 422s with a clear message naming the actual current occurrence's date, materialized and completed the 1st (current) occurrence successfully, confirmed the 2nd was then completable, and confirmed the identical in-order/out-of-order behavior for Skip on the 3rd/4th occurrences. The rejection's `ItemError::Invalid` renders as the existing red error-toast fragment in place — reads cleanly, no UI polish needed.

2. **Migrating an item onto `item_series` changes its day-to-day visibility — resolved 2026-08-14, design confirmed with the user, not yet implemented.** Today, completing a recurring item immediately creates its next occurrence as a real row, visible in the flat `/tasks`/`/events` list right away. Under `item_series`, the next occurrence stays virtual until materialized (a click, or dashboard/calendar browsing) — and the flat list was already scoped (Stage 8) to never render virtual occurrences at all, on the reasoning that a flat list has no date-range concept to bound how much of a series' future to show. Rather than accept the regression or force eager materialization on completion (breaking the lazy-materialization model as a special case for migrated items), the resolved fix revisits that Stage 8 reasoning: it only applies to *ranges*, not to showing a series' single **current** occurrence, which is range-independent (`current_occurrence_date` is a pure function of the cursor, no window needed). That's structurally the same thing the flat list already shows for a legacy recurring item today — exactly one row, always "the active instance."
   - Implementation is cheap: `list_virtual_occurrences_for_project_unchecked` already injects the current occurrence into its results regardless of the caller's queried window (the Stage 9 backlog-exemption logic) — so no new query is needed, just call the existing function with any range, filter for `is_current`, and merge those rows into the flat Tasks list the same additive-merge pattern the dashboard/calendar already use (materialized ones are already covered by the normal item query, so no double-render; unmaterialized ones render as a virtual entry with the same materialize-on-click behavior Stage 5 built).
   - This fully preserves the lazy-materialization model — nothing is written to the DB just because it's displayed, only when actually clicked.
   - Visual treatment, resolved 2026-08-15: no special badge/marker distinguishing a virtual current row from a real item row — same row style as everything else in the list. The row's link just materializes-then-navigates on click (the existing Stage 5 pattern) rather than opening an edit form directly, but nothing about how it *looks* sets it apart.
   - Scope: Task-series only. Event series have no cursor/completion concept, so there's no single "current" occurrence to single out — the flat Events list stays untouched, unaffected by this.
   - Like gap #1's cross-cutting decision above, this revisits part of an already-shipped scoping choice (Stage 8, `80ca7b4`) — but only its "flat list is entirely out of scope" reasoning, not any code it wrote, so there's nothing to unwind, only new wiring to add.
   - **Committed 2026-08-15**: implemented as designed above, via its own refined-planning pass (`AskUserQuestion`, one question) before code. `list_virtual_occurrences_for_project_unchecked` needed no changes — its existing backlog-exemption logic already injects a Task series' `current_occurrence_date` into its results regardless of the queried range, confirmed by reading the function directly, so `project_tasks_page` (`src/web_ui/project_tasks/handlers.rs`) calls it with a degenerate `Utc::now()..Utc::now()` range and filters `.filter(|occ| occ.item_type == ItemKind::Task && occ.is_current)`. One question resolved before writing code: the doc's original "looks identical to a real row, no badge" visual-treatment note (above) doesn't fit this list — real rows here use the full `Row` component (complete-checkbox, Edit/Delete, subordinate-task picker), none of which apply to something that doesn't exist as an item yet, and forcing `Row` itself to grow a virtual-mode branch would add complexity to a component every other screen (Events, Simple Lists) also depends on. Resolved by reusing the existing bespoke virtual-row style already proven on the dashboard and Tasks calendar (italic name, opacity, Current badge, Skip button) rather than inventing a third visual treatment or hacking `Row`. New `ProjectTaskVirtualRow` (`src/web_ui/project_tasks/templates.rs`, modeled directly on `project_dashboard::ProjectDashboardVirtualRow`, minus the type-symbol badge since this list is Task-only already) → new template `templates/project_tasks/virtual_row.html` (own `task-list-virtual-` id prefix, distinct from the calendar's `cal-virtual-` and the dashboard's `proj-dash-virtual-`; no "Planned" branch, since every row built here is current by construction). New `render_rows_with_virtual` (`src/web_ui/project_tasks/mod.rs`), added alongside — not in place of — `render_rows`, since `render_rows` has three other call sites in the same module (children/subordinate task lists) where virtual occurrences don't apply; mirrors `project_dashboard::render_rows`'s exact merge pattern (render each kind to `(timestamp, html)` pairs, concatenate, sort by timestamp, discard the timestamp). No service-layer or migration changes; `cargo build`/`cargo check`/full suite (333 tests, unchanged — this module has no existing unit tests for `render_rows`/`list_project_tasks` to extend, matching this codebase's existing precedent that web_ui-layer-only changes are verified by smoke test) all clean. Manually smoke-tested end-to-end against a throwaway local server/DB (existing dev server on 3001 left untouched): created a Task-typed series, confirmed its current occurrence rendered in the flat `/tasks` list with the italic/Current/Skip treatment and no checkbox/Edit/Delete; confirmed clicking it materialized and redirected to the real task's detail page, after which the virtual row was gone and an ordinary real row took its place with no double-render; confirmed Skip removed the virtual row and advanced the series to its next current occurrence without creating any item; confirmed a separately created Event-typed series' occurrences never appeared in the flat Tasks list.

3. **No `item_series` equivalent of child-subtree carry-forward — resolved 2026-08-14, design confirmed with the user, not yet implemented.** `get_or_materialize_occurrence` (`src/service/item_series.rs`) creates a bare item with no child-copying at all, unlike legacy's `clone_children` (`src/service/items.rs`). Confirmed design: when materializing occurrence N, look up the single most-recently-materialized prior occurrence for that series (one indexed query — not an unbounded scan) and, if it has children, clone them onto N via the same `due_offset_days`/`deadline_from_offset` recompute `clone_children` already does — except non-destructively (leave the old subtree in place, since materialized occurrences stay as real history rows under `item_series`, unlike legacy's archive-and-delete). No new schema — no series-level "template children" concept — children just carry forward occurrence-to-occurrence via the existing `parent_item_id`/`due_offset_days` mechanism already on `Item`. The current-occurrence-only restriction (gap 1's cross-cutting decision above) is what makes "the most recently materialized prior occurrence" unambiguous here: since occurrences can now only ever settle strictly in order, it's always exactly the one immediately before N, never a backlog item that got jumped past out of order.

4. **CSV import's `recurrence`/`recurrenceBasis` columns — resolved 2026-08-15: dropped entirely, not carried forward.** `import.rs` writes these straight onto a plain `Item` today via the legacy mechanism, with no `item_series` awareness at all; teaching it to instead create-a-series-plus-materialize-first-occurrence per recurring row was considered and rejected as disproportionate to import's actual use case (bulk-loading a batch of items, not authoring one carefully-configured recurring thing). Once the legacy mechanism is retired, a CSV row with `recurrence` set is a **hard error for that row** — same treatment as any other invalid row today (collected into that row's `ImportItemResult { success: false, error: ... }`, not a silent drop/ignore) — rather than gaining any series-creation capability. Anyone wanting a recurring series creates it once via the UI/CLI/MCP series endpoints, not through a bulk-import row. `ITEM_IMPORT_TEMPLATE_CSV`'s header and `docs/prl-user-guide.md`'s import section need updating to drop those columns once this ships.

5. **Raised 2026-08-15, resolved and committed the same day: does the legacy per-creation template-trigger mechanism (CLAUDE.md's Events section — `event_type` matching that auto-copies a template's children onto a newly created `Event`) still fire correctly, and non-redundantly, when an `item_series` occurrence materializes?** `get_or_materialize_occurrence` (`src/service/item_series.rs`) calls `project_items::create_project_item` to create the occurrence's backing `Item`, and that call path is the same one the legacy trigger mechanism hooks into (`create_item`/`create_team_item` in `src/service/items.rs`/`team_items.rs`) — so an `EVENT`-typed series with `event_type` set would have refired the legacy template-copy trigger on *every single materialization*, independently of and potentially conflicting with gap 3's own child-carry-forward design (which copies from the series' own most-recently-materialized prior occurrence, not a template).

   **Committed 2026-08-15**: `event_type` is now unconditionally rejected on *any* item series (Task or Event alike), not just narrowed on Task as it was before — `validate_series_event_type` (`src/service/item_series.rs`) dropped its `item_type` parameter entirely and now always errors if `event_type` is `Some(..)`, regardless of series kind. This was previously a Stage-7c-era check that specifically *permitted* `event_type` on an `Event`-typed series; that permission is what's being revoked here. Rationale (per the user, "call it unsupported for now — maybe it will be worth the effort later"): a `Task` series already covers "I want this to recur," so nobody needs the legacy trigger's indirection layered on top of series materialization, and disallowing it sidesteps the "are these two mechanisms in sync" question entirely rather than requiring reconciliation — deliberately left as a single loosen-able validation check (not a schema change), so revisiting it later (e.g. once gap 3 lands, or if the two mechanisms are made to cooperate on purpose) is cheap. Touch-point pass: the series create form (`templates/project_item_series/new_page.html`) had its `event_type` field and the corresponding half of its `itemType`-toggle script removed entirely (no edit form exists to touch, same as gap 1); `prl series create`/`update` lost their `--event-type` flag outright (`todo-cli/src/series.rs`) rather than keeping a flag that would always 400 — `Get`'s printed `event type:` line was kept, since a pre-existing series created before this change can still have a stored value worth displaying; the MCP `create_item_series`/`update_item_series` tool schemas and their case handlers (`mcp-server/src/index.ts`) dropped the `eventType` property and its request-body wiring, with both tools' descriptions updated to say so explicitly. Two new unit tests (`create_series_rejects_event_type_on_event_series`, `update_series_rejects_event_type_on_event_series`) cover the combination that used to be allowed; `update_params()`'s test fixture default flipped from `Some("meeting")` to `None` so the general happy-path update test doesn't trip the new rejection, with the two existing Task-side rejection tests updated to set `event_type` explicitly instead of relying on that old default. 333 tests, full suite green; `cargo build`/`check`, `task web-styles`, and mcp `npm run build` all clean. Smoke-tested against a throwaway server: confirmed the create page's HTML has zero `eventType`/`event-type` occurrences, and confirmed the API now 400s on `eventType` set with `itemType: EVENT` (previously the one case this was allowed) with message "event_type is not currently supported on an item series".

**Status as of 2026-08-15: gap 1 (`COMPLETION_DATE` basis), gap 2 (flat Tasks list's current-occurrence visibility), and item 5 (`event_type` disallowed on any series) are all committed — see their own "Committed 2026-08-15" notes above. The cross-cutting current-occurrence-only Complete/Skip decision is also committed (Stage 10a, see above).** Remaining, each needing its own refined-planning pass before code, per this doc's own per-sub-stage discipline: gap 3 (child-subtree carry-forward on materialization — item 5's resolution removes the "does the legacy trigger conflict with this" question, since `event_type` can no longer be set on a series at all), gap 4 (CSV import's hard-error-on-`recurrence`), and the actual Stage 10 core: the one-time legacy-data migration plus retiring the old mechanism's code paths. No changes yet to `import.rs`.

**Stage 11 — `sourceEventId` → series support** (unchanged in substance from the original Stage 7, renumbered)
- Let a Task's `sourceEventId` point at a series, resolved to "whichever occurrence is current" at read time (materializing on demand if needed) — now trivially the `EVENT`-typed case of the already-generalized `item_series`, rather than needing bespoke resolution logic of its own.
- Commit: "Support sourceEventId pointing at a recurring series".

### Notes on this redesign's staging

- 7a/7b/7c mechanically replay Stage 4's own a/b split, one level bigger — a rename touches more files than net-new addition did, per the touch-point checklist.
- Stage 9 is where this redesign's real novelty lives (task series never existed before) — budget the most planning time there, the same caution the original doc gave the old Stage 7.
- Stage 10 is deliberately last: everything through Stage 9 is purely additive (a new `item_type` option, nothing existing forced to change), matching this doc's own "Stages 1–3 have no user-visible effect" precedent. Only Stage 10 touches data/behavior that already exists in production.
- As with the rest of this doc: not scheduled or approved for implementation beyond this planning pass. Each stage still needs its own refined-planning pass before code, per the doc's closing convention below.

---

## Superseded: original Stage 7/8 sketch (kept for history, see scope change above)

**Stage 7 — `sourceEventId` → series support** (covers change #5 above)
- Let a Task's `sourceEventId` point at a series, resolved to "whichever occurrence is current" at read time (materializing on demand if needed).
- Highest-risk stage: touches the template-trigger matching and `copy_template_children` logic described in CLAUDE.md's Events section, which today assume a concrete Event item id. Budget extra time for this one; don't bundle it with anything else.
- Commit: "Support sourceEventId pointing at a recurring series".

**Stage 8 — Retire the superseded single-row path, import/export** (covers change #6 above)
- Decide whether Event's old `recurrence`/`recurrence_basis` auto-advance-on-read mechanism (see the sibling doc, if it was built) gets retired now that series cover the same need for Events specifically — Tasks keep the old mechanism regardless, since this doc only ever addressed Events.
- Update `src/service/import.rs`/the PRL CSV format if series should be importable/exportable.
- Commit: "Retire single-row recurrence path for Events now superseded by series".

### Notes on staging itself (original sketch — see "Notes on this redesign's staging" above for the current plan)

- Stages 1–3 have no user-visible effect and are safe to land well ahead of the rest — they're pure addition with no existing call site touched.
- Stage 4 is the first point where the touch-point checklist (Smithy/codegen/CLI/MCP/docs) applies in full; everything before it is Rust-only.
- Stage 7 is deliberately last-but-one, not folded into stage 4 or 5, because it's the one place this feature reaches into *existing* behavior (event-trigger matching) rather than only adding new surface — isolating it makes it easier to revert on its own if it goes sideways.
- As in the rest of this doc: none of this is scheduled or approved for implementation. This breakdown exists so that *if* it's picked up, the work doesn't have to be re-planned from scratch first.

### Notes on staging itself

- Stages 1–3 have no user-visible effect and are safe to land well ahead of the rest — they're pure addition with no existing call site touched.
- Stage 4 is the first point where the touch-point checklist (Smithy/codegen/CLI/MCP/docs) applies in full; everything before it is Rust-only.
- Stage 7 is deliberately last-but-one, not folded into stage 4 or 5, because it's the one place this feature reaches into *existing* behavior (event-trigger matching) rather than only adding new surface — isolating it makes it easier to revert on its own if it goes sideways.
- As in the rest of this doc: none of this is scheduled or approved for implementation. This breakdown exists so that *if* it's picked up, the work doesn't have to be re-planned from scratch first.

**Each stage below should get its own refined-planning pass before implementation, not just be executed off this doc's one-paragraph sketch.** Stage 1 got this treatment (a dedicated plan-mode session, resolving specific open questions like whether to hand-roll occurrence generation or adopt the `rrule` crate, and the exact `MonthlyDay`-clamp-vs-skip semantics, via `AskUserQuestion` before any code was written — see the git history around `40eca9d`). Stage 2 did not: it was implemented directly off this doc's method-name list and table sketch, with several unstated design calls (upsert semantics via `ON CONFLICT`, storing `recurrence` as a raw string rather than a structured column, `mark_exdate` preserving an existing `item_id` rather than clearing it, index shape) made ad hoc during implementation rather than surfaced and confirmed first — acceptable there mainly because every call mirrored an existing, already-reviewed precedent (`ProjectRepo`, `ItemRepo`, `ActivityLogRepo`). Later stages (especially 4, 5, and the original 7 — see the sizing/risk notes above) have design surface this doc doesn't resolve at all (exact Smithy shape, how virtual-vs-materialized renders in the calendar grid, the `sourceEventId`-to-series resolution semantics) and should not be implemented directly from their one-paragraph descriptions here. The same holds for the redesign's own Stages 7a–11 above: this planning pass resolved the three cross-cutting forks (trigger model, retirement, naming) but explicitly left per-stage questions open (Stage 9's template-children design, Stage 10's data-migration approach) — resolve those with their own `AskUserQuestion` pass before writing code, the same discipline Stage 1 got and Stage 2 skipped.
