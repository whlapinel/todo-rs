# Calendar day-drawer + calendar-as-default view

## Context

Today, four screens have a month-grid calendar view as an *alternate* view next to a list view: the cross-project Home dashboard (`main_dashboard.rs`, `/web/dashboard` list vs. `/web/dashboard/calendar`), the per-project dashboard (`project_dashboard.rs`), the per-project Tasks screen (`project_tasks/`), and the per-project Events screen (`project_events/`). Each calendar page is a month grid (`calendar_page.html`) with a plain `<div id="calendar-day-list">` sitting *below* the grid; clicking a day cell does an htmx `hx-get` that swaps only that div's contents (`calendar_day_panel.html`), not the grid itself.

This plan:

1. **Makes the calendar the default/primary view** for the two screens that keep one at all — the cross-project Home dashboard and the per-project dashboard (see point 3).
2. **Replaces the below-grid day panel with a slide-in drawer** (Tailwind Plus's `overlays/drawers/08-contact-list-example.html`, available locally at `~/tailwind-ui-html/html/overlays/drawers/08-contact-list-example.html`), with its own prev/next-*day* arrows (distinct from the grid's prev/next-*month* arrows) and an All/Tasks/Events tab bar plus an "assigned to me" toggle.
3. **Retires the per-project Tasks and Events calendar views entirely.** Once the per-project dashboard's drawer has All/Tasks/Events tabs, it's a strict superset of what the Tasks-only and Events-only calendars showed — a day's full task+event list, filterable down to just one type. Tasks and Events keep their **list** view only; the sidebar links for Tasks/Events go straight to the (unchanged) filterable list, with no calendar toggle anymore.
4. **Fixes the selected-day highlight to update client-side** on every day click / drawer arrow click, without ever re-fetching or re-rendering the month grid.

None of this retires the Home/per-project dashboard **list** pages, `assigned_items.rs` ("assigned to me" cross-project list), or the Events child-item sub-routes (`.../events/:id/children`) — all raised and explicitly deferred during planning; see "Explicitly out of scope" below.

This plan also folds in three related, already-written entries from `docs/issues_and_features.md` that the user asked to include because they intersect directly with this work:

- *"Need button on each row with vertical ellipses icon that gets dialog with row actions (reschedule/duplicate/delete/assign/move/edit/etc.) — `#action-dialog` in `base.html`."* — the Tailwind Plus drawer example's own row markup has exactly this (an `el-dropdown`/`el-menu` "•••" button per row), which is what prompted revisiting it now. See Stage 1.
- *"Events list rows still lack the 'duplicate' action... Fold into a vertical-ellipses menu alongside edit/delete/assign once that exists."* — small parity add-on inside Stage 1, once the menu exists.
- *"Clicking cell in calendar will render the correct day but the highlighted cell doesn't update properly for some reason."* — this is the exact bug Point 4 above fixes; resolved by Stage 2.
- *"Calendar should have same filter as list - assigned to user should be default, can select assigned to any."* — implemented in Stages 3/4.

Once this plan is fully implemented, move those four bullets out of `docs/issues_and_features.md` (into `docs/archived/archived_issues_and_features.md`), and reconsider the remaining "Let's shrink the calendar view by at least 50%... put the month beside the list [on desktop]" bullet — the "put list beside grid" half is moot once the list is a drawer overlay rather than page content competing for space; the "shrink the grid" half is still a legitimate, independent, small follow-up worth keeping open.

**Deferred, final-stage rename:** the user wants the per-project and cross-project "Dashboard" screens renamed to "Calendar" (nav label, page titles, etc.) once this is otherwise complete — see Stage 7. Split at the start of Stage 7 into two stages, per the user's explicit request: Stage 7 does the display-text-only rename (nav sidebar label, page `<title>` blocks, user-facing headings); Stage 8 does the full route/module/struct rename (`/web/dashboard` → `/web/calendar`, `main_dashboard.rs`/`project_dashboard.rs` module names, `MainDashboardRow`/`ProjectDashboardRow` struct names, etc.) that Stage 7's own note had left open pending user confirmation.

### Explicitly out of scope (raised, then deliberately deferred by the user while planning)

- **Not retiring** the Tasks list page or the Events list page — only their *calendar* views (Stage 5). Both list pages keep every bit of their current functionality (filters, New button, row actions), unchanged.
- **Not retiring** either dashboard's **list** page — calendar becomes the *default* landing view (Stage 6), but the list stays reachable at an explicit URL, unchanged in functionality.
- **Not retiring** `assigned_items.rs` (`/web/assigned-items`), even though the dashboard calendars gain their own "assigned to me" toggle in this plan. Both can coexist.
- **Not touching** `.../events/:id/children` (an Event's own sub-item management) — a detail-page concept, unrelated to this list/calendar work.
- **Not a CLI/MCP change** — `prl` and the MCP server have no calendar-rendering concept at all; nothing here needs a `todo-cli`/`mcp-server` touch-point.

### Confirmed design decisions

- **All/Tasks/Events tabs and the assigned-to-me toggle live on the two dashboard calendars only** (there's no other per-project calendar left once Stage 5 retires the Tasks/Events ones, and the cross-project Home calendar is dashboard-shaped by definition).
- **The month grid's per-day tally badge is always unfiltered** (counts everything, regardless of the drawer's active type-tab). Only the drawer's own list body reflects the type-tab filter. This avoids coupling a same-day tab click to a month-wide re-render/re-fetch, and keeps the "how much is on this day, roughly" badge simple. The **assigned-to-me toggle**, unlike the type tabs, *does* reload the whole calendar page (full month tally + drawer both become "mine"-scoped) — it's a rarer action than clicking through days, matching how the existing list views already reload fully when `assignedToAny`/`showComplete` change, so no new partial-reload architecture is needed for it.
- **New-item creation**: the per-project dashboard calendar gains a small "New" button + Task/Event type picker (Stage 3), since it's about to become the primary way to browse a project's tasks/events by day, and today's dashboards have no creation entry point at all. The Tasks/Events list pages keep their own existing "New task"/"New event" buttons unchanged. The **cross-project Home** calendar does *not* get a New button — item creation is inherently project-scoped and Home has no single natural project to create into.
- **The row-actions "⋮" menu (Stage 1) reuses the existing `#action-dialog` convention**, not the Tailwind Plus example's `<el-dropdown>`/`<el-menu>` custom elements. This app has zero existing usage of `@tailwindplus/elements` anywhere (confirmed by grep) — `error_dialog.html`'s reference to "`el-dialog` primitives" is borrowing that component family's *Tailwind transition classes* (`data-closed:`, `data-enter:`, etc.) onto this app's own plain native `<dialog>`, not loading the actual JS library. Introducing the real custom-element library would mean a new CDN script dependency this app has deliberately avoided everywhere else (see `CLAUDE.md`'s "no browser-side SPA" architecture note); a native `<dialog>` swapped via `#action-dialog` gives the same "click ⋮, small menu of actions appears" UX without one.
- **Default-routing swap (Stage 6) keeps the old `.../calendar` URL alive as a redirect to the new base path**, rather than deleting it — cheap, and avoids breaking anything that already links directly to `.../calendar` (bookmarks, the sidebar's own historical links if any survive an incomplete audit).

## Why staged, and what's in scope per stage

Same process as `docs/archived/google-calendar-import-plan.md` / `docs/archived/project-abstraction-plan.md`: each stage is independently landable, leaves the app's running behavior unchanged (or only additively changed) until wired up, and is done in its own session — **compact/clear context between stages**, with this file as the only thing that survives the handoff. Before ending a stage, update that stage's section below with an **Implementation notes** entry: exact names if they ended up different, deviations and why, test/build status, anything discovered that changes a later stage's assumptions.

**"Independently landable" describes the code, not a standing instruction to `git commit`.** Per this repo's global CLAUDE.md/git policy, commits only happen when the user explicitly asks — the user's own framing of this task ("we'll commit and clear context between stages and only push when complete with the entire plan") is that explicit ask, scoped to this plan: commit at the end of each stage, never push until every stage is done and the user separately confirms.

1. **Row actions → a "⋮" menu.** Touches the shared `templates/components/row.html` (already used by every list *and* every existing day panel), so it's foundational and worth landing before the drawer work below inherits it. No behavior change beyond consolidating icons into one menu; Events gains "duplicate" parity as a small included add-on.
2. **Drawer shell, prev/next-day arrows, and client-side selected-day highlight — proven on the per-project Dashboard calendar** (chosen as the sole per-project calendar going forward, and already mixed-type, so the drawer mechanism is proven against both Task and Event rows in one pass — no throwaway work on a screen that's about to be retired). No filtering yet in this stage; the drawer shows exactly what today's day panel shows, just in the new shell.
3. **Add All/Tasks/Events tabs, the assigned-to-me toggle, and a New-item button** to the now-drawer-equipped per-project Dashboard calendar.
4. **Port the drawer (+ tabs + toggle, no New button) to the cross-project Home Dashboard calendar.**
5. **Retire the Tasks and Events calendar views.** Delete their `calendar_page.html`/`calendar_day_panel.html` templates, handlers, and routes; remove the "Calendar view" link from both list pages; audit for any other reference to `.../tasks/calendar` or `.../events/calendar` before deleting.
6. **Default-view routing swap**, for the two screens that still have both a list and a calendar view (Home dashboard, per-project dashboard): the base URL now serves the calendar page; the list view moves to an explicit `.../list` route; old `.../calendar` URLs redirect to the base path.
7. **Rename "Dashboard" to "Calendar" — display text only.** Nav sidebar label, page `<title>` blocks, and any other user-facing "Dashboard" wording become "Calendar". Deliberately last among the functional stages, per the user's explicit "defer that rename til the very end." No route/module/struct changes in this stage.
8. **Rename "Dashboard" to "Calendar" — full route/module rename.** `/web/dashboard` → `/web/calendar` (and the per-project equivalent), `main_dashboard.rs`/`project_dashboard.rs` module names, `MainDashboardRow`/`ProjectDashboardRow` struct names, `nav.rs`'s `section_href` path segment, the Stage-6 redirect handler names, and every internal link/redirect target that assumed the old path. Added as its own stage per the user's explicit request when Stage 7 began, splitting off the larger-blast-radius half Stage 7's own note had left pending confirmation.

---

## Stage 1 — Row actions consolidated into a "⋮" menu

`templates/components/row.html` currently renders, inline, whichever of these `Option<String>` URL fields the caller supplied: `reschedule_url` (calendar icon → `hx-get` into `#action-dialog`), `assign_url` (same pattern), `duplicate_url` (`hx-post` with `hx-confirm`), plus an unconditional-if-`!is_imported` delete button (`hx-delete`) and a `skip_url` "Skip" text button for series occurrences. Replace all of these (delete/skip excepted — see below) with a single trailing "⋮" (vertical-ellipsis) button per row that `hx-get`s a small actions-menu fragment into `#action-dialog`, following the exact mechanism `reschedule_url`/`assign_url` already use today (so the *fetch* mechanism is unchanged — only what's fetched, and when, changes).

**New menu fragment** (`templates/components/row_actions_menu.html`, new): a small list of action links/buttons inside `#action-dialog`'s existing shell, visually modeled on the Tailwind Plus example's `el-menu` popover styling (rounded panel, `divide-y`, hover states) but built from plain markup, not the `<el-menu>` custom element (see Confirmed design decisions above). Rows:

- **Edit** — plain link to the item's edit route (where one exists), closes the dialog by navigating away.
- **Reschedule** — present iff `reschedule_url.is_some()`; `hx-get`s that URL into `#action-dialog` itself (same target, swaps the menu for the existing reschedule form — no new plumbing needed, `#action-dialog`'s content is just replaced).
- **Assign** — present iff `assign_url.is_some()`; same swap-in-place pattern.
- **Duplicate** — present iff `duplicate_url.is_some()`; `hx-post` with the existing `hx-confirm`, same as today.
- **Delete** — always present unless `is_imported`; `hx-delete` with the existing `hx-confirm`, targeting `#item-{id}` exactly as today (not `#action-dialog` — deleting closes the dialog *and* removes the row in one swap; needs `hx-on::after-request` or a small script to also call `action-dialog.close()` after a successful delete, since the delete's own `hx-target`/`hx-swap` isn't the dialog).

**`skip_url`'s "Skip" button is left where it is, outside the menu** — it's a frequent, single-click action for a recurring occurrence (not a rare/destructive one like the others), and burying it a click deeper would be a regression, not a consolidation. Same reasoning for the **complete checkbox** — untouched, stays inline.

**Events duplicate parity** (the `docs/issues_and_features.md` add-on): `src/web_ui/project_events/templates.rs`'s `ProjectEventRow::from_item` currently hardcodes `duplicate_url: None`. Wire it up the same way `ProjectTaskRow` already does (needs a `duplicate_project_event`-shaped handler + route mirroring `project_tasks`'s existing `/tasks/:id/duplicate`, if one doesn't already exist under a different name — confirm during this stage before assuming a new handler is needed).

**Row width**: with reschedule/assign/duplicate/delete collapsed into one "⋮" button, `row.html`'s per-row markup shrinks from up to 5 trailing icon buttons to at most 2 (Skip, ⋮) plus the checkbox — worth double-checking the row still reads well at the day-drawer's narrower width (the drawer panel is `max-w-md`, notably narrower than a full list page) once Stage 2 exists to test it against, but the row markup itself doesn't need to know anything about being inside a drawer vs. a list page.

**Files touched:** `templates/components/row.html`, `templates/components/row_actions_menu.html` (new), `src/web_ui/project_events/templates.rs` + `handlers.rs` + `mod.rs` (duplicate parity, if not already present), `templates/base.html` (small JS addition: closing `#action-dialog` after a successful delete triggered from inside it).

**Verification:** `cargo build`, `task web-styles`; live click-through (via the `run` skill or Playwright) on at least one Tasks row and one Events row confirming: ⋮ opens the menu, Reschedule/Assign swap the dialog content in place, Duplicate still confirms and duplicates, Delete still confirms, removes the row, and closes the dialog, Skip and the checkbox are unaffected and still work directly from the row.

### Implementation notes (fill in before ending this stage)

**Menu-opening mechanism deliberately deviates from the plan's literal "hx-get into `#action-dialog`" wording, and no new server route was added for the menu itself.** By the time a row renders, `Row` (`src/web_ui/components/row.rs`) already carries every URL the menu needs (`edit_url` — new field, `reschedule_url`, `assign_url`, `duplicate_url`, `item_url` for delete) — there was nothing a round trip to the server would add. Instead `row.html` renders the menu's markup inline via `{% include "components/row_actions_menu.html" %}` inside a hidden `<template id="row-actions-menu-{{ id }}">` sitting right next to the "⋮" button. The button's `onclick="openRowActionsMenu('{{ id }}')"` (new function in `base.html`) copies that template's `innerHTML` into `#action-dialog`, calls `htmx.process(dialog)` (required — htmx only auto-binds `hx-*` attributes on elements it swaps in itself or that were present at initial page load, not on a plain `.innerHTML =` assignment) so the menu's own Reschedule/Assign (`hx-get` into `#action-dialog` itself, unchanged mechanism)/Duplicate (`hx-post`)/Delete (`hx-delete`) buttons work exactly as before, then calls `showModal()`. This avoids adding a new handler+route per screen (tasks/events/simple-lists) purely to serve a menu whose entire content was already known. Reschedule/Assign/Duplicate/Delete's own request mechanics inside the menu are byte-for-byte the same `hx-get`/`hx-post`/`hx-delete` attributes the old per-icon buttons used.

**Delete-closes-dialog**: implemented as a generic `htmx:afterRequest` listener in `base.html` (closes `#action-dialog` when a successful request's triggering element used `hx-delete` and is inside the open dialog) rather than a per-button `hx-on::after-request` attribute — matches the plan's file-touch list (`base.html`) and is reusable by any future delete-from-within-dialog action, not just this one.

**`edit_url` added to `Row`** (`components/row.rs`) — didn't exist before (the row's `item_url` always went to the read-only detail page; "Edit" previously only ever appeared as a link *on* that detail page). Populated per screen: `ProjectTaskRow` gates it on `!item.complete` (mirrors the task detail page's own Edit-link gate); `ProjectEventRow` gates it on `!google_event_id.is_some()` (mirrors the existing imported-event gate `reschedule_url` already used); `ProjectSimpleItemRow` always sets it (simple items have no complete/imported concept blocking edit).

**Events duplicate parity**: added `duplicate_project_event_form` (`project_events/handlers.rs`, mirrors `project_tasks::handlers::duplicate_project_task_form` exactly, using `require_event` instead of `require_task`) and its route `POST /projects/:project_id/events/:item_id/duplicate` in `main.rs`. `ProjectEventRow::from_item`'s `duplicate_url` is gated on `!google_event_id.is_some()`, same as `reschedule_url`/the new `edit_url`. No new `project_events_list_url` helper was added — the redirect target is inlined (`format!("/web/projects/{project_id}/events")`), matching that file's existing convention of inlining URLs rather than a shared helper (unlike `project_tasks/handlers.rs`, which does have one).

**`icons/ellipses.html`** already existed in the repo (unused) and was reused as-is for the "⋮" button — no new icon file needed. `icons/calendar.html`/`assign.html`/`duplicate.html`/`delete.html` are still used elsewhere (`project_dashboard/row.html`, `project_item_series/row.html`, `project_templates/row.html`, list-page headers) and were left untouched.

**Verification actually performed**: `cargo build`, `cargo fmt -- --check` (confirmed no new formatting drift beyond this stage's own two lines, which were then `rustfmt`'d — the repo has substantial *pre-existing* unrelated formatting drift in other files, left untouched), `cargo test` (455 passed), `task web-styles`. Live-tested via Playwright against a local `cargo run` (had to rebind off the default port — something else in the dev environment answers on `localhost:3001` with HTTP 426, so the server was run on `127.0.0.1:3901` for this session only; not a code change) in `TODO_AUTH_MODE=caddy` + `TODO_DEV_EMAIL` dev-bypass mode: opened the ⋮ menu on a team-backed-project Task row (all 5 actions present incl. Assign), confirmed Reschedule swaps the dialog content in place with prefilled values, confirmed Duplicate creates a "(copy)" row and redirects, confirmed Delete's browser-native `hx-confirm` fires, accepting it removes the row and closes `#action-dialog` (verified the dialog element disappears from the accessibility tree, not just visually), confirmed the checkbox and Skip button (outside the menu, untouched markup) still work directly — including hitting the pre-existing "cannot complete an unassigned team item" 422 guard correctly retargeted into `#error-dialog`, unrelated to this change. Created and duplicated a throwaway Event via the ⋮ menu on the Events screen to confirm the new Duplicate parity works end-to-end (menu showed Edit/Reschedule/Duplicate/Delete, no Assign — correct for Events), then deleted the throwaway data afterward. Zero console errors throughout other than an unrelated `favicon.ico` 404.

---

## Stage 2 — Drawer shell, day arrows, and client-side highlight — proven on the per-project Dashboard calendar

**Stage-1 follow-up fix, done ahead of the Stage 2 work below (not itself part of Stage 2): the row-actions "⋮" menu now opens as a popover anchored beside the clicked button, not a centered modal dialog.** `templates/components/row_actions_menu.html` no longer renders inside `#action-dialog` (the centered, backdrop-dimmed `<dialog>`) — `base.html` now has a second persistent element, `<div id="action-menu-popover" popover>` (the native HTML Popover API — `showPopover()`/`hidePopover()`/`:popover-open`, no `@tailwindplus/elements` or any other library), reset with `m-0 p-0 border-0 fixed inset-auto` to strip the UA popover default (centered, bordered) styling. `openRowActionsMenu(rowId, btn)` (now takes the clicked button as a second argument) copies the row's hidden `<template>` markup into the popover, calls `htmx.process()`, `showPopover()`s it, then a new `positionActionMenuPopover()` right-aligns it under the button using `getBoundingClientRect()` math (flips above if there's no room below, clamps horizontally) — plain viewport-relative positioning, not CSS anchor positioning (newer/less supported than the Popover API itself). The menu fragment itself dropped its backdrop div, centered-flex wrapper, and Cancel button (the popover's own light-dismiss — outside click/Escape — replaces Cancel, matching the `el-menu` precedent it's modeled on); Reschedule/Assign still `hx-get` into `#action-dialog` exactly as before (that's a separate, unrelated `<dialog>` for the actual form), and a single generalized `htmx:afterRequest` listener (replacing the old delete-specific one, which is now dead code since no `hx-delete` element lives in `#action-dialog` anymore) closes the popover after any successful request from an element inside it — covering Reschedule/Assign/Duplicate/Delete and Edit's own boosted navigation with one listener instead of per-button `onclick`s. Verified via `cargo build`, `task web-styles`, and a live Playwright check (Reschedule correctly closed the popover and opened `#action-dialog` with the form) before the user asked to stop using Playwright for verification going forward — see the new note in `CLAUDE.md`'s Commands section; all verification from this point on in the plan is `cargo build`/`cargo test`/`task web-styles` plus direct `curl` inspection of rendered HTML, not a browser.

**Drawer shell** (`templates/project_dashboard/calendar_page.html` + a restyled `templates/project_dashboard/calendar_day_panel.html`): replace the plain `<div id="calendar-day-list">` with a persistent `<dialog id="day-drawer">`, following this app's existing `#action-dialog`/`#error-dialog` convention exactly (a stable-id `<dialog>` present in the page's own markup — not `base.html`, since the drawer is calendar-page-specific — whose *innerHTML* is what gets swapped by htmx; JS opens it via `showModal()` on swap, mirroring the existing `htmx:afterSwap` listener in `base.html` that already does this for `#action-dialog`/`#error-dialog`). Markup/transition classes adapted from `~/tailwind-ui-html/html/overlays/drawers/08-contact-list-example.html` (the `el-dialog-panel`-style slide-in-from-the-right treatment, `data-closed:translate-x-full`, etc. — same Tailwind transition-class family `error_dialog.html` already borrows, not the custom element itself).

Day-cell buttons' `hx-target`/`hx-select` change from `#calendar-day-list` to the drawer's inner content wrapper; the `.../dashboard/calendar/day?date=...` fragment route is unchanged in URL shape but now renders drawer-shaped content (header with the date label + close button + prev/next-day arrows, then the row list body — still unfiltered by type in this stage, exactly matching what `day_list_rows` renders today).

**Prev/next-*day* arrows** (new, distinct from the grid's existing prev/next-*month* arrows): rendered by the day-fragment handler itself (`project_dashboard_calendar_day_fragment` in `src/web_ui/project_dashboard.rs`), which already knows the requested `date` — compute `date - 1 day`/`date + 1 day` server-side and render two arrow buttons at the top of the drawer, each `hx-get`ing the same fragment route with the adjacent date, targeting the same drawer content wrapper. This mirrors the month-nav arrows' own already-established shape (plain `<a>`/`hx-get` pair either side of a label) so no new client-side date math is needed anywhere.

**Client-side selected-day highlight (fixes the `docs/issues_and_features.md` bug directly)**: give every day-cell button in the month grid a `data-date="{{ day.date }}"` attribute (it doesn't have one today). Add one generic listener in `base.html` (alongside `toggleSidebar()`'s precedent): on `htmx:afterSwap`, if the swapped-in root element carries a `data-date` attribute (the day-drawer fragment's own root gets one — this is what both a day-cell click *and* a drawer prev/next-day-arrow click end up triggering, since both ultimately swap the same fragment), find the calendar grid's own day-cell button matching that date (`[data-date="..."]` within the current page's grid) and toggle the highlight classes (`ring-2 ring-inset ring-indigo-500`) onto it, removing them from whichever cell currently has them. One listener, reusable unmodified by Stage 4's port to the Home dashboard.

This is a pure client-side class toggle — it never re-fetches or re-renders the month grid itself, satisfying the plan's "without re-rendering the entire month view" requirement directly.

**Files touched:** `templates/project_dashboard/calendar_page.html`, `templates/project_dashboard/calendar_day_panel.html`, `src/web_ui/project_dashboard.rs` (`project_dashboard_calendar_page`/`project_dashboard_calendar_day_fragment`, prev/next-day date computation, `data-date` on the fragment root), `templates/base.html` (the new generic highlight-updater listener), `styles/input.css` if any new utility combination needs registering, `task web-styles`.

**Verification:** `cargo build`, `task web-styles`; live click-through: open the drawer from a day cell, confirm the highlight moves there; use the drawer's own prev/next-day arrows repeatedly and confirm the month-grid highlight tracks each move; confirm via browser dev-tools network tab that none of this ever issues a request to the month-page route itself (only `.../calendar/day` fragment requests); exercise a checkbox toggle on both a Task and an Event row, a Skip, and the Stage 1 "⋮" menu's Reschedule/Assign (dialog-inside-a-dialog case) from a row *inside* the open drawer, confirming both dialogs stack/close correctly and don't fight each other's `htmx:afterSwap`-driven auto-open behavior.

### Implementation notes (fill in before ending this stage)

**No route/URL changes** — `.../dashboard/calendar` and `.../dashboard/calendar/day` keep their exact existing shapes; only what they render changed.

**Drawer content is re-rendered wholesale on every swap, matching the plan's design exactly**: `templates/project_dashboard/calendar_day_panel.html` now renders the *entire* `#day-drawer` innerHTML each time — backdrop div, sliding-panel wrapper (`translate-x-full` → `group-open/dialog:translate-x-0`), header (date label + prev/next-day arrows + close button), and the row list — rather than just an inner content div, because the sliding-panel transition classes need to be present on every swap, not just the first (the outer `<dialog id="day-drawer" class="group/dialog ...">` itself is what persists in `templates/project_dashboard/calendar_page.html` across swaps, exactly like `#action-dialog`/`#error-dialog`). The day-cell buttons' `hx-target`/`hx-select` changed from `#calendar-day-list` (now deleted) to `#day-drawer`/`unset`; base.html's existing `htmx:afterSwap` dialog-auto-open listener already handles opening it (its `e.detail.target.matches('dialog')` branch matches directly, since the target *is* the dialog this time) — no new open-triggering code needed beyond the inline `showModal()` script `calendar_page.html` renders for a hard/bookmarked `?date=...` load (the one case with no htmx swap to trigger the listener).

**Rust-side**: `ProjectDashboardCalendarDayPanelTemplate` gained `project_id: String` and `drawer: Option<DayDrawerData>` (a new small struct — `date_iso`/`selected_date_label`/`prev_date`+`prev_year`+`prev_month`/`next_date`+`next_year`+`next_month`, all populated together so the template needs only one `{% if let Some(drawer) = drawer %}` guard rather than one per field), replacing the old flat `selected_date_label: Option<String>`. A new `render_day_drawer(project_id, date: Option<NaiveDate>, day_rows) -> Result<String, ItemError>` builds this template and is the single call site both `project_dashboard_calendar_page` (full page — computes `day_drawer_html: String` embedded via `{{ day_drawer_html|safe }}`, plus a `has_selected_date: bool` gating the inline `showModal()` script) and `project_dashboard_calendar_day_fragment` (the fragment route — returns `render_day_drawer(...)`'s output directly as the response body) go through, so the two can never render the drawer differently. Prev/next-day dates and their *own* year/month (not the grid's currently-displayed year/month) are computed from the selected date itself (`date ± 1 day`, then that new date's own `.year()`/`.month()`) — deliberately, since a day-arrow click can cross a month boundary the displayed grid never re-fetches; using the arrow-adjusted date's own year/month keeps the pushed URL correct for a subsequent hard reload even though the grid itself stays showing whatever month was already open (matching the plan's explicit "never re-fetch/re-render the month grid" requirement).

**Highlight fix**: every day-cell button already needed `data-date="{{ day.date }}"` (new) per the plan; the drawer fragment's own outermost element also carries `data-date="{{ drawer.date_iso }}"`. `base.html` gained one new `htmx:afterSwap` listener, scoped to `e.detail.target.id === 'day-drawer'` specifically (so an unrelated `#page` boost, which also fires `htmx:afterSwap`, never mistakes some other `[data-date]` element on the page for a selection change) — it removes the ring classes from whichever grid cell currently has them and adds them to the cell matching the swapped-in date. This never touches the month grid's own markup/re-renders it; `day.is_selected` (server-computed, unchanged) still seeds the *initial* full-page-load highlight, exactly as planned.

**Deviation from the plan's literal file list**: no changes were needed to `styles/input.css` — every new class used (`fixed`, `inset-y-0`, `translate-x-full`, `group-open/dialog:translate-x-0`, etc.) already exists as a plain Tailwind utility or was already in use elsewhere in the app (the `group/dialog` convention, borrowed from `error_dialog.html`/`action-dialog`).

**Verification actually performed**: `cargo build` (clean, only pre-existing unrelated warnings), `cargo fmt -- --check` (confirmed zero drift in the new code — the diff hits in `project_dashboard.rs` are all pre-existing lines untouched by this stage, same as Stage 1's precedent), `cargo test` (455 passed, unchanged), `task web-styles`. Per the user's mid-Stage-2 request (see the new `CLAUDE.md` note above), verification did **not** use a browser/Playwright — instead: started the dev server locally (`TODO_BIND=127.0.0.1:3901`, `TODO_AUTH_MODE=caddy` + `TODO_DEV_EMAIL` dev-bypass, same as Stage 1) and used `curl` to fetch and inspect the rendered HTML directly: confirmed the month grid's day cells carry `data-date`; confirmed a no-`?date` page load renders an empty (comment-only) `#day-drawer` with no `showModal()` script; confirmed a `?date=2026-08-22` load renders the full drawer markup inside `#day-drawer` *and* the inline `showModal()` script *and* `ring-2 ring-inset ring-indigo-500` on the matching grid cell; confirmed the `.../calendar/day?date=...` fragment route's raw response is exactly the same drawer markup (backdrop, sliding panel, header with correct prev/next-day `hx-get`/`hx-push-url` URLs computed from the requested date, row list) that the full page embeds. This confirms correct server-side rendering and wiring but does **not** confirm the client-side JS (positioning math, the highlight listener, dialog open/close, transition animation) actually behaves correctly in a real browser — that part is left for the user's own manual check, per their instruction.

---

## Stage 3 — Tabs, assigned-to-me toggle, and New-item button on the per-project Dashboard calendar

**All/Tasks/Events tabs**: rendered in the drawer header below the date label/arrows, styled on the Tailwind Plus example's tab markup (`border-indigo-500 text-indigo-600` for the active tab, `border-transparent text-gray-500 hover:...` for inactive — copied classes, no new component). Each tab `hx-get`s the same day-fragment route with an added `?type=all|task|event` query param, targeting a *narrower* wrapper than the full drawer content (`#day-drawer-list`, say) so switching tabs doesn't re-fetch/re-render the date label or arrows — just the row list. `project_dashboard_calendar_day_fragment` gains a `type: Option<String>` query param, applied as a filter over `day_list_rows`'s existing item-kind data (`ItemKind::Task`/`Event`) before rendering. Per the Confirmed design decisions above, this filter **does not** affect the month grid's per-day tally.

**Assigned-to-me toggle**: mirrors `project_dashboard.rs`'s existing list-view `assigned_to_any: Option<String>` convention exactly (`None`/absent = filtered to the requester; present = show everyone's). Default is "mine" (matching `docs/issues_and_features.md`'s explicit spec and the list view's existing default). Unlike the type tabs, this toggle is a plain link/form that reloads the **whole calendar page** (`project_dashboard_calendar_page`'s own `CalendarQuery` gains `assigned_to_any: Option<String>`, threaded into `build_calendar_days`'s tally computation the same way `project_dashboard_list_rows`'s list-view filtering already threads it) — so the month tally and the drawer's default (unfiltered-by-type) view are both consistently "mine" or "everyone's" after toggling.

**New-item button**: a "New" button in the calendar page's header opening a tiny inline type picker (Task / Event — two links, no new dialog needed, just two `<a href="{{ ... }}/tasks/new">`/`.../events/new` links styled as a small dropdown or a two-button group) — reuses the existing, unchanged `/tasks/new`/`/events/new` create-form routes; no prefill of the selected day in this first pass (flag as a nice-to-have deferred, not required for this stage).

**Files touched:** `templates/project_dashboard/calendar_page.html`, `templates/project_dashboard/calendar_day_panel.html`, `src/web_ui/project_dashboard.rs` (`CalendarQuery`, `build_calendar_days`, `day_list_rows`, both calendar handlers).

**Verification:** `cargo build`, `task web-styles`; live click-through: tabs filter the drawer body only (network tab confirms no month-page refetch); assigned-to-me toggle changes both the tally and the drawer and does reload the page; New button reaches both create forms.

### Implementation notes

**Type tabs reuse the existing fragment route with a narrower `hx-select`/`hx-target`, exactly as the plan proposed — no new route.** `project_dashboard_calendar_day_fragment` still renders the *whole* drawer (header, arrows, tabs, list) on every request; a tab click's `hx-target="#day-drawer-list" hx-select="#day-drawer-list" hx-swap="outerHTML"` (mirroring `project_tasks/detail_view.html`'s existing `#item-{id}-view` precedent — same id on both `hx-select` and `hx-target`, `outerHTML` swap so the extracted node replaces the old one instead of nesting) picks just the list wrapper back out of that full response. Day-cell clicks and the prev/next-day arrows are unaffected — they still swap the whole `#day-drawer` via `hx-select="unset"`, as in Stage 2.

**`CalendarQuery` gained `r#type: Option<String>` (a raw identifier — serde's derive strips the `r#` prefix when computing the default field name, so the query param is plain `type`) and `assigned_to_any: Option<String>`, shared by both calendar handlers** as the plan's file list implied. `parse_type_filter`/`active_type_label` (new, near `type_symbol`) are the single source of truth for "?type=" → `Option<ItemKind>` and back to the canonical `"all"/"task"/"event"` string the template compares against — an unrecognized or missing value always normalizes to `"all"`, so a tab click and a hand-typed URL can never disagree on what's active.

**Deviation from a literal reading of `render_rows`' assigned-to-any filter: gated on `is_team_project`, not applied unconditionally.** Tracing `assigned_to_user_id()` (`src/domain/item.rs`) confirmed CLAUDE.md's Points-section claim in code, not just prose: a personal-project item never gets a `TeamAssignment` at all (`src/storage/sqlite/mod.rs`'s row mapping only builds one `if assigned_to_user_id.is_some() || points.is_some()`, and `service::project_items::create_project_item`'s personal branch has no slot for either field), so `assigned_to_user_id()` is unconditionally `None` for every personal-project item. `render_rows`' own filter (`assigned_to_any || item.assigned_to_user_id() == Some(user_id)`, no `is_team_project` gate) would, applied literally to the calendar's new "mine"-by-default, empty every personal project's tally and drawer unless the user manually checked "Assigned to Any" — confirmed live (see below) before adding the gate, not assumed. Both new filters (`day_list_rows`'s `mine` closure, `build_calendar_days`'s identical one) instead read `!is_team_project || assigned_to_any || assigned == Some(user_id)`, matching what CLAUDE.md already says the field means. `render_rows` itself (the flat list's existing filter) was left untouched — out of this stage's file list — but this is a real, pre-existing latent bug there worth a follow-up; noting it rather than silently patching a file this stage doesn't otherwise touch.

**`assignedToAny` is threaded through every drawer-internal control, not just the page-level checkbox** — day-cell clicks, the month-nav prev/next arrows, and the drawer's own prev/next-day arrows and type tabs all carry `&assignedToAny=1` forward when active (each reads it off `ProjectDashboardCalendarPageTemplate::assigned_to_any` or `DayDrawerData::assigned_to_any`, the latter threaded through `render_day_drawer`'s new `assigned_to_any` parameter). Without this, switching tabs or paging days would have silently reset the filter back to "mine" every time, contradicting the plan's "both the tally and the drawer... reload the page" framing (the drawer's day-to-day navigation isn't a full page reload, so it has to carry the state explicitly instead of relying on a query param default). Same treatment for `type`: the day-drawer's own arrows preserve whatever tab was active (`drawer.active_type`) so paging days doesn't reset the tab; a fresh day-cell click always starts at "All" (no prior tab context exists to carry forward), matching the plan's implicit "opening a new day starts unfiltered" assumption.

**`DayDrawerData` gained `date_year`/`date_month`** (the *selected* date's own year/month, distinct from the grid's displayed month — same rationale Stage 2 already established for `prev_year`/`prev_month`) so the tabs' `hx-push-url`/plain `href` can build a correct bookmarkable URL without the day-panel template needing the grid's currently-displayed month as a separate input.

**New-item button implemented as a plain two-link segmented group** (`+ Task` / `+ Event`, styled like the existing month-nav prev/next segmented control), per the plan's explicitly-sanctioned simpler option — no dropdown, no JS. No date prefill, also per the plan's "flag as a nice-to-have deferred" note.

**Askama note**: an inline array-literal `{% for (value, label) in [(...), (...)] %}` loop (my first draft, to avoid three near-identical tab blocks) has no established precedent anywhere in this codebase's templates and wasn't risked — written out as three explicit `<a>` blocks instead, consistent with how `presets`/other small fixed option-sets are already handled elsewhere (either a real `Vec` field from Rust, or literal repetition).

**Verification actually performed**: `cargo build` (clean), `cargo fmt -- --check` (`git stash` comparison confirmed the same 6 pre-existing drift locations in `project_dashboard.rs` exist before and after this stage's diff, at shifted line numbers — zero new drift introduced), `cargo test` (455 passed, unchanged), `task web-styles`, `task check`. Per `CLAUDE.md`'s no-Playwright instruction, live verification was `curl` against a locally-run `cargo run` (`TODO_BIND=127.0.0.1:3902`, `TODO_AUTH_MODE=caddy` + `TODO_DEV_EMAIL` dev-bypass) against the one real project available (a personal project — no team-backed project existed to test the `is_team_project`-gated branch directly, so that half is verified by code-reading + the `render_rows` precedent it mirrors, not a live request): confirmed the header renders the New Task/Event links, the Assigned-to-Any checkbox, and List view link; confirmed `.../calendar/day?date=...&type=task`/`type=event`/`type=all` each return exactly the expected subset of rows after creating a throwaway same-day Task + Event (deleted afterward); confirmed the active tab's `border-indigo-500` class lands on the correct tab for each `type` value; confirmed exactly one `#day-drawer-list` element in the fragment response (no accidental nesting from the `hx-select`/`hx-swap="outerHTML"` combination); confirmed prev/next-day arrows and tabs all carry `type`/`assignedToAny` forward in their `href`/`hx-get`/`hx-push-url` URLs; confirmed the month grid's per-day tally is unfiltered by type but does reflect `assignedToAny` (identical mine-vs-any tallies on this personal project, as expected per the `is_team_project` gate above — initially mis-read as a bug from a too-narrow `grep -B/-A` context window before finding the real `entry_count` span a few lines further down); confirmed `/tasks/new` and `/events/new` both return 200. Did not verify client-side rendering (transitions, popover positioning) in an actual browser — left for the user's own check, per their standing instruction.

---

## Stage 4 — Port the drawer (+ tabs + toggle) to the cross-project Home Dashboard calendar

Same shape as Stages 2–3 combined (drawer port + tabs + assigned-to-me toggle), applied to `templates/main_dashboard/calendar_page.html` + `calendar_day_panel.html` + `src/web_ui/main_dashboard.rs`'s `main_dashboard_calendar_page`/`main_dashboard_calendar_day_fragment`/`build_calendar_days`/`day_list_rows`/`gather_calendar_data`. No New-item button here (see Confirmed design decisions above). One extra wrinkle Stage 3 didn't have: `main_dashboard.rs` already applies its own baked-in assignment restriction independent of any toggle (per `CLAUDE.md`'s Web UI section: "Tasks show unrestricted on a personal project but only assigned to requester on team-backed one") — confirm during this stage exactly how the new toggle composes with that existing baked-in rule (most likely: the toggle only relaxes the team-backed-project restriction when set to "everyone," and has no effect on personal-project tasks, which were never restricted to begin with) rather than assuming they're independent of each other.

**Files touched:** `templates/main_dashboard/calendar_page.html`, `templates/main_dashboard/calendar_day_panel.html`, `src/web_ui/main_dashboard.rs`.

**Verification:** `cargo build`, `task web-styles`; live click-through mirroring Stage 3's, plus a specific check that the assigned-to-me toggle behaves sensibly across a mix of a personal project and a team-backed project's items in the same cross-project view.

### Implementation notes

**The "extra wrinkle" the plan flagged resolved exactly as the plan's own "most likely" guess predicted, confirmed by tracing the code rather than assumed.** `main_dashboard.rs`'s cross-project `is_included(kind, is_team_project, assigned_to, user_id)` already baked in "a Task is unrestricted on a personal project but restricted to the requester's own assignment on a team-backed one" (the CLAUDE.md-documented rule), independent of any toggle. It gained one new parameter, `assigned_to_any: bool`, and exactly one line changed: `ItemKind::Task => !is_team_project || assigned_to == Some(user_id)` became `ItemKind::Task => !is_team_project || assigned_to_any || assigned_to == Some(user_id)`. This *relaxes* the team-backed restriction when the toggle is on, and is a no-op for personal-project tasks (never restricted) and Events (never restricted either) — precisely the composition the plan predicted, not an independent second filter layered on top. The flat list view (`list_main_dashboard_rows`, which has no assigned-to-me toggle of its own) passes `assigned_to_any: false` at both its call sites, preserving its existing behavior byte-for-byte.

**Architecture ended up simpler than `project_dashboard`'s Stage 3 precedent, not a deviation but a consequence of how this file was already structured.** In `project_dashboard.rs`, `build_calendar_days`/`day_list_rows` take *unfiltered* `due_items`/`virtual_occurrences` and apply their own `mine` closure internally, because that module's tally and list are built from a single project's raw item list. `main_dashboard.rs`'s `gather_calendar_data` was already the single per-request data-gathering pass shared by both the page and fragment handlers, pre-filtering every due-item/occurrence through `is_included` before either `build_calendar_days` or `day_list_rows` ever sees them — so making `is_included` itself `assigned_to_any`-aware was sufficient to make *both* the tally and the drawer list "mine"-or-"everyone's"-aware with zero changes to `build_calendar_days`, and `day_list_rows` only needed the new `type_filter: Option<ItemKind>` parameter (no `mine` closure of its own, since that filtering already happened upstream). One `assigned_to_any` parameter threaded through `gather_calendar_data` covers both concerns.

**`DayDrawerData`/`render_day_drawer`/`MainDashboardCalendarDayPanelTemplate` are duplicated from `project_dashboard`'s Stage 2/3 equivalents, minus a `project_id` field** — this screen's routes are all `/web/dashboard/calendar...`, never project-scoped, so every URL in the ported `calendar_day_panel.html` template is a plain path with no `{{ project_id }}` interpolation, otherwise byte-for-byte the same structure (backdrop, sliding panel, header with prev/next-day arrows, All/Tasks/Events tabs, `#day-drawer-list`). `parse_type_filter`/`active_type_label` are likewise duplicated verbatim from `project_dashboard.rs`, per this file's own established precedent (see its very first doc comment on `preset_range`) of duplicating small per-screen helpers rather than widening a sibling module's visibility.

**No New-item button** — per the plan's Confirmed design decisions (item creation is project-scoped; Home has no single natural project to create into), `main_dashboard/calendar_page.html`'s header carries only the month-nav arrows, the Assigned-to-Any checkbox, and the List view link — no `+ Task`/`+ Event` segmented control, unlike `project_dashboard/calendar_page.html`'s.

**`base.html`'s Stage 2 highlight listener and dialog-auto-open listener needed zero changes** — both are already scoped by element id (`#day-drawer`) rather than URL path, exactly as Stage 2's implementation notes anticipated ("One listener, reusable unmodified by Stage 4's port to the Home dashboard").

**Verification actually performed**: `cargo build` (clean, same pre-existing unrelated warnings as every prior stage), `cargo fmt -- --check` (zero drift in `main_dashboard.rs` specifically — confirmed via `grep -A2 "main_dashboard.rs"` on the check output; the tool's full output shows only pre-existing drift in unrelated files, e.g. `calendar_subscriptions.rs`/`main.rs`, untouched by this stage), `cargo test` (455 passed, unchanged), `task web-styles` (zero diff in `static/style.css` — every class used already existed in the app, same as Stage 2). Per `CLAUDE.md`'s no-Playwright instruction, live verification was `curl` against a locally-run `cargo run` (`TODO_BIND=127.0.0.1:3903`, `TODO_AUTH_MODE=caddy` + `x-token-user-email` header, a throwaway SQLite DB under the scratchpad dir) against the one project available (a personal project — same limitation Stage 3 hit, no team-backed project existed to exercise the `is_team_project`-gated half of `is_included` live, so that half is verified by code-reading only, not a live request): confirmed every day-cell carries `data-date`; confirmed a `?date=...` full-page load renders the drawer with the `showModal()` script and the "All" tab active by default; created a throwaway same-day Task + Event (deleted afterward) and confirmed `.../calendar/day?date=...&type=all/task/event` each return exactly the expected subset, with the correct tab's `border-indigo-500` class active in each response; confirmed the month grid's per-day tally correctly counted both (`2`) under the default "mine" view; confirmed the drawer's prev/next-day arrows and All/Tasks/Events tabs all carry `type`/`assignedToAny` forward in their `hx-get` URLs (e.g. requesting `type=task&assignedToAny=1` produced prev/next-day links and tab links that all preserved both params); confirmed the List view link and the pre-existing checkbox-toggle route (`PUT .../dashboard/projects/:id/items/:id`) are unaffected by this stage's changes. Did not verify client-side rendering (transitions, the highlight listener actually moving in a real browser) — left for the user's own check, per their standing instruction.

---

## Stage 5 — Retire the Tasks and Events calendar views

Now that the per-project dashboard's drawer (Stage 3) shows a day's tasks and events together, filterable to just one type via the All/Tasks/Events tabs, the Tasks-only and Events-only calendars are redundant — delete them rather than maintaining three overlapping day-browsing UIs per project.

- Delete `templates/project_tasks/calendar_page.html`, `templates/project_tasks/calendar_day_panel.html`, and their handlers (`project_tasks_calendar_page`, `project_tasks_calendar_day_fragment`) + route registrations in `src/main.rs`. Same for `project_events/calendar_page.html`, `calendar_day_panel.html`, `project_events_calendar_page`, `project_events_calendar_day_fragment`.
- Remove the "Calendar view" link from `templates/project_tasks/list_page.html` and `templates/project_events/list_page.html` (the Tasks/Events list pages otherwise keep every bit of their current functionality, unchanged).
- **Audit before deleting**: grep the whole repo (templates, handlers, `docs/`) for any reference to `.../tasks/calendar` or `.../events/calendar` that isn't the two things above — e.g. confirm no reschedule/quick-catchup flow, no "New task" success-redirect, and no `nav.rs`/sidebar entry depends on either route surviving.
- `build_calendar_days`/`day_list_rows`/related helper functions in `project_tasks/mod.rs` and `project_events/mod.rs` may become entirely dead code once their only callers (the deleted handlers) are gone — remove them too rather than leaving unused code behind, unless something else in either module still calls them (double-check; don't assume).

**Files touched:** `templates/project_tasks/calendar_page.html` (deleted), `templates/project_tasks/calendar_day_panel.html` (deleted), `templates/project_events/calendar_page.html` (deleted), `templates/project_events/calendar_day_panel.html` (deleted), `src/web_ui/project_tasks/handlers.rs` + `mod.rs` + `templates.rs`, `src/web_ui/project_events/handlers.rs` + `mod.rs` + `templates.rs`, `src/main.rs` (route removals), `templates/project_tasks/list_page.html`, `templates/project_events/list_page.html`.

**Verification:** `cargo build` (confirm no leftover dead-code warnings beyond expected ones, no broken template references), `task check`; live click-through confirming the Tasks and Events list pages work exactly as before minus the now-removed "Calendar view" link, and that `.../tasks/calendar`/`.../events/calendar` return a clean 404 rather than a broken page.

### Implementation notes (fill in before ending this stage)

**Straight deletion, no surprises found during the audit.** Deleted the four templates (`templates/project_tasks/calendar_page.html`/`calendar_day_panel.html`, `templates/project_events/calendar_page.html`/`calendar_day_panel.html`) and the four route registrations in `src/main.rs` (`/projects/:project_id/tasks/calendar[/day]`, `/projects/:project_id/events/calendar[/day]`). Removed the "Calendar view" icon-link from both `templates/project_tasks/list_page.html` and `templates/project_events/list_page.html` — the Events list page's separate "Manage Google Calendars" link (a different feature, `calendar-subscriptions`) was left untouched.

**Handlers removed**: `project_tasks_calendar_page`/`project_tasks_calendar_day_fragment` (`src/web_ui/project_tasks/handlers.rs`, plus their now-unused `CalendarQuery` struct and `day_panel_label` helper) and `project_events_calendar_page`/`project_events_calendar_day_fragment` (`src/web_ui/project_events/handlers.rs`, same two helper shapes). **Templates removed**: `ProjectTasksCalendarPageTemplate`/`ProjectTasksCalendarDayPanelTemplate`/`CalendarDay` (`project_tasks/templates.rs`) and their Events counterparts (`project_events/templates.rs`).

**Per the plan's explicit instruction, the calendar-only helpers in each module's `mod.rs` were confirmed dead (grepped for every call site first, not assumed) and removed rather than left behind**: `project_tasks::{day_list_rows, build_calendar_days, grid_start_for, local_date_to_utc, prev_month, next_month}` and the Events equivalents `project_events::{day_list_rows, build_calendar_days, grid_start_for, local_date_to_utc, prev_month, next_month, calendar_date}` — every one of these had its sole call site inside the handlers just deleted. `render_rows_with_virtual` (`project_tasks/mod.rs`) is *not* dead — `list_task_rows_for_project` (the flat Tasks list) still calls it — so it and its `in_list_view` parameter were left alone; only a stale doc-comment line referencing the now-gone `day_list_rows` call site was reworded to talk about subordinate/children lists instead (still an accurate description of the other reason that parameter exists).

**Import cleanup cascaded further than the plan's file list implied, confirmed via `cargo build`/rust-analyzer diagnostics rather than guessed**: removing the calendar handlers left `list_project_tasks` unused in `project_tasks/handlers.rs` (the flat list goes through `list_task_rows_for_project` instead, which calls it internally) and left `Duration`/`NaiveDate`/`NaiveTime`/`Datelike`/`Query` unused across both `handlers.rs` files and both `mod.rs` files (everywhere the calendar code was the only bare, non-`chrono::`-qualified consumer of those chrono types). All trimmed; `DateTime`/`Utc` stayed since both modules' `create_params_from_form`/`update_params_from_form` still use them directly.

**One deviation from a literal `cargo fmt` run**: running `cargo fmt` after the edits reformatted several pre-existing drift spots in files this stage didn't otherwise touch (`calendar_subscriptions.rs`, `calendar_sync.rs`, `item_series.rs`, a migration file, two `storage/sqlite` files, `project_dashboard.rs`, `project_item_series/handlers.rs`) plus a couple of pre-existing drift spots inside the two touched `mod.rs` files themselves (a multi-line-wrapped call and an `if let` above the code actually deleted). Reverted all of those back to their pre-stage formatting — confirmed via a before/after `cargo fmt -- --check` diff that the only formatting changes left in the diff are ones `rustfmt` made to lines this stage's own edits touched (the trimmed `use` blocks), and that the drift `cargo fmt -- --check` still reports afterward is byte-for-byte the same pre-existing set (accounting for line-number shifts from the deletions, and for entries that vanished because they were *inside* the deleted code).

**Verification actually performed**: `cargo build` (clean — only the same pre-existing unrelated dead-code/unused-import warnings present before this stage, all in files untouched here: `item_series.rs`, `users.rs`, `item.rs`, `project.rs`, `recurrence.rs`, `team.rs`, `user.rs`, `storage/sqlite/mod.rs`, `components/row.rs`), `cargo fmt -- --check` (see deviation note above — zero new drift), `cargo test` (455 passed, same count as Stage 4's ending state — no test referenced the retired calendar handlers/templates), `task web-styles` (zero diff in `static/style.css` — nothing calendar-icon-link-specific was the sole user of any Tailwind class), `task check` (`cargo check`, clean). Repo-wide grep confirmed no other reference to `.../tasks/calendar`/`.../events/calendar` or the four deleted handler function names survives outside this plan file itself and `docs/archived/*` (historical, untouched). One stale mention in `CLAUDE.md` (`project_events/`'s screen-list entry, which still advertised "its own... `/events/calendar` month-grid view") was corrected. Did not verify in a live browser (curl/click-through) — per `CLAUDE.md`'s no-Playwright instruction and since this stage is pure deletion with no new runtime behavior to exercise; a 404 on the two retired route prefixes follows directly from removing them from `build_web_router()`, confirmed by reading the route table rather than by request.

---

## Stage 6 — Default-view routing swap (calendar becomes the default for the two remaining calendar screens)

For each of the two screens that still have both a list and a calendar view (Home dashboard, per-project dashboard):

1. Move the existing list handler to an explicit `.../list` route (e.g. `/web/projects/:project_id/dashboard/list`), keeping its own logic untouched.
2. Make the existing calendar handler serve the *base* path (e.g. `/web/projects/:project_id/dashboard`) instead of `.../calendar`.
3. Keep the old `.../calendar` path alive too, as a redirect to the base path (per the Confirmed design decisions above) — cheap insurance against any stale link.
4. Update the calendar page's "List view" link to point at the new `.../list` path; update the list page's "Calendar view" link to point at the (now-default) base path instead of `.../calendar`.
5. **Audit every internal `Redirect::to(...)`/form-post/link across both modules that currently targets the base path**, since the base path's *meaning* just changed from "the list" to "the calendar." A handler that redirects back to the base path after a mutation (e.g. after toggling a dashboard row's completion) almost certainly means "go back to the list I was on" and must be updated to target `.../list` explicitly, not silently start landing users on the calendar instead. This is the stage's single highest-risk item — grep each module for every reference to its own base path (not just the obvious redirect helpers) before considering the stage done.
6. `src/web_ui/nav.rs`'s `section_href`/sidebar links, the header logo link, and `GET /`'s redirect target already point at each section's base path — confirm (don't assume) that none of them hard-code a `.../calendar` or `.../list` suffix that would need updating too.

**Files touched:** `src/main.rs` (route table for both screens), `src/web_ui/main_dashboard.rs`, `src/web_ui/project_dashboard.rs`, `templates/main_dashboard/*.html`, `templates/project_dashboard/*.html`, `src/web_ui/nav.rs` (if the audit in step 6 finds anything).

**Verification:** `cargo build`, `task check`; live click-through of both screens' base URLs now landing on the calendar; both list pages still reachable via their new `.../list` link; both old `.../calendar` URLs still work (redirects); every mutation (complete/skip/delete/duplicate/reschedule/assign, from both the list view and the new drawer) still redirects/reloads to the page the user was actually on, never silently bouncing them to the other view.

### Implementation notes (fill in before ending this stage)

**Route table swap, exactly per the plan's own sketch.** In `src/main.rs`, for both screens: the base path (`/dashboard`, `/projects/:project_id/dashboard`) now routes to the calendar handler (`main_dashboard_calendar_page`/`project_dashboard_calendar_page`); a new `.../dashboard/list` route routes to the old list handler (`main_dashboard_page`/`project_dashboard_page`), completely unchanged internally; `.../dashboard/calendar` now routes to a new redirect handler (below); `.../dashboard/calendar/day` (the day-drawer fragment route) was **left exactly where it was** — it's an htmx-only fragment endpoint nothing ever bookmarks or links to directly, so there was no reason to also rename it to `.../dashboard/day`, and leaving it put kept this stage's diff to route *table* entries only, no handler-signature changes.

**New redirect handlers, one per screen, added right next to each screen's own page handler**: `redirect_main_dashboard_calendar` (`main_dashboard.rs`) and `redirect_project_dashboard_calendar` (`project_dashboard.rs`), both taking `RawQuery` and forwarding a non-empty query string onto the new base path (`Redirect::to(&format!("{base}?{q}"))`), falling back to a bare redirect when there's no query — so a bookmarked `.../calendar?year=...&date=...` still lands on the exact same day/month once redirected, not just the current month. `RawQuery`/`Redirect` were newly imported into both files for this (no prior use of `RawQuery` anywhere in the codebase).

**The audit in step 5/6 found exactly the two internal-redirect risks the plan predicted, and no others.** Both `main_dashboard/page.html` and `project_dashboard/page.html`'s own preset-select/show-complete/assigned-to-any-checkbox `hx-get`s targeted the base path for their in-place self-reload — these now target `.../dashboard/list` instead, or they'd have started silently reloading the calendar on every filter change. Each list page's own "Calendar view" link, and each calendar page's own "List view" link, were swapped to point at the (now-default) base path and the new `.../list` path respectively. Nothing else internally redirected to either screen's base path: `redirect_to_current_page` (`project_item_series/handlers.rs`) already redirects to `HX-Current-URL` rather than any hardcoded path, and the `view=project-dashboard`/`view=main-dashboard` branches of the Skip/Unskip/complete-occurrence handlers (`project_tasks/handlers.rs`, `project_item_series/handlers.rs`) already rebuild their list in place via an HTML fragment response, never a redirect — so none of them needed touching. `toggle_project_dashboard_item_complete`/`toggle_main_dashboard_item_complete` (the row-checkbox PUT handlers) likewise only ever return a rendered row fragment, never a redirect.

**Every other internal URL inside the calendar page/day-panel templates that used to say `.../dashboard/calendar?...` (bookmarkable `href`s, `hx-push-url` targets: month-nav prev/next, the assigned-to-me checkbox, day-cell push-url, the day-drawer's own prev/next-day arrows, and the All/Tasks/Events tabs) now says `.../dashboard?...` instead** — a single `dashboard/calendar?` → `dashboard?` substitution across all four calendar templates (`main_dashboard`/`project_dashboard` × `calendar_page.html`/`calendar_day_panel.html`), safe because every `hx-get` that actually calls the *fragment* route uses the distinct `dashboard/calendar/day?` pattern (with `/day` in between), which the substitution doesn't touch — confirmed by grepping the exact before/after set of matches in all four files before applying it, not assumed.

**`nav.rs`'s `section_href`/the header logo link (`nav.html`)/the sidebar's "Home" link (`nav_sidebar_inner.html`) needed zero changes**, confirmed rather than assumed: all three already point at each screen's base path (`/web/dashboard`, `section_href(SidebarSection::None, project_id)` → `/web/projects/{project_id}/dashboard`) with no `/calendar` or `/list` suffix hardcoded anywhere, so they land on the calendar automatically now that the base path's handler changed — exactly the "no update needed" case the plan's step 6 allowed for.

**One doc fix beyond the plan's own file list**: `CLAUDE.md`'s Web UI section described `main_dashboard.rs`/`project_dashboard.rs` as `/web/dashboard[/calendar]`/`/projects/:project_id/dashboard[/calendar]` — stale as of this stage (the calendar is the base path now, not an optional suffix) — reworded to describe the new base/`.../list`/`.../calendar`-redirect shape, same as Stage 5's precedent of fixing a stale CLAUDE.md route mention it found along the way.

**Verification actually performed**: `cargo build` (clean, same pre-existing unrelated warnings as every prior stage), `cargo fmt -- --check` (confirmed zero new drift — the diff hits in `project_dashboard.rs` are the same 6 pre-existing drift locations at shifted line numbers, none overlapping the new redirect function; `main_dashboard.rs` has zero diff at all), `cargo test` (455 passed, unchanged), `task check` (clean), `task web-styles` (zero diff in `static/style.css` — nothing here needed a new Tailwind class, only URL string changes). Per `CLAUDE.md`'s no-Playwright instruction, live verification was `curl` against a locally-run `cargo run` (`TODO_BIND=127.0.0.1:3904`, `TODO_AUTH_MODE=caddy` + `x-token-user-email` header, a throwaway SQLite DB under the scratchpad dir): confirmed `/web/dashboard` now renders the calendar page (`<title>Dashboard Calendar · Todo</title>`, `id="day-drawer"` present) and `/web/dashboard/list` renders the list (`<title>Dashboard · Todo</title>`, `#main-dashboard-list` present); confirmed `/web/dashboard/calendar` redirects (303) to `/web/dashboard`, and with a query string attached, redirects to `/web/dashboard` with that same query string preserved; confirmed the same three things for a real project's `/web/projects/:id/dashboard`, `.../dashboard/list`, and `.../dashboard/calendar` (with `year`/`month`/`date`/`assignedToAny` all preserved through the redirect); confirmed the calendar page's "List view" link points at `.../dashboard/list` and the list page's "Calendar view" link plus its preset-select `hx-get` all point at the base path/`.../dashboard/list` respectively; confirmed the day-drawer fragment route (`.../dashboard/calendar/day?date=...`) still works unchanged and that the day-cell's own `hx-push-url` now bakes in the base path (`.../dashboard?year=...&date=...`), not the old `.../calendar` path. Did not verify client-side rendering (transitions, the highlight listener) in an actual browser — left for the user's own check, per their standing instruction; a real project's calendar page click-through (day-cell → drawer → arrows/tabs) should be spot-checked live before considering the routing swap fully proven end-to-end, since this stage's own verification only exercised the routes/redirects/links via `curl`, not the drawer's client-side JS against the new URLs.

---

## Stage 7 — Rename "Dashboard" to "Calendar" (display text only)

Deliberately last among the functional stages, per the user's explicit "defer that rename til the very end." Scoped, at the user's explicit request when this stage began, to **display text only** — nav sidebar label, page `<title>` blocks, and any other user-facing "Dashboard" wording become "Calendar", with no route/module/struct changes. The full route/module rename this stage's earlier draft had left open pending confirmation is now its own Stage 8 below, not part of this one.

Every user-facing occurrence of the literal word "Dashboard" (confirmed by grep across `templates/`, not assumed):

- `templates/nav_sidebar_inner.html` — the per-project sidebar link's `Dashboard` text (the `dashboard_href` link, i.e. `section_href(SidebarSection::None, ...)`). The cross-project Home dashboard's own sidebar link already reads "Home", not "Dashboard" — left untouched, since it isn't the word being renamed.
- `templates/teams/detail_page.html` — the team detail page's `Dashboard` button linking to that team's backing project's dashboard.
- `templates/main_dashboard/calendar_page.html` — `{% block title %}Dashboard Calendar · Todo{% endblock %}`.
- `templates/main_dashboard/page.html` — `{% block title %}Dashboard · Todo{% endblock %}` and the page's own `<h2>Dashboard</h2>` heading.
- `templates/project_dashboard/calendar_page.html` — `{% block title %}Dashboard Calendar · Todo{% endblock %}`.
- `templates/project_dashboard/page.html` — `{% block title %}Dashboard · Todo{% endblock %}` and the page's own `<h2>Dashboard</h2>` heading.

No Rust string literals say "Dashboard" (confirmed by grep over `src/`) — every occurrence is template-only, so this stage touches no `.rs` file.

**Files touched:** `templates/nav_sidebar_inner.html`, `templates/teams/detail_page.html`, `templates/main_dashboard/calendar_page.html`, `templates/main_dashboard/page.html`, `templates/project_dashboard/calendar_page.html`, `templates/project_dashboard/page.html`.

**Verification:** `cargo build` (Askama re-embeds templates at compile time), `task check`; grep confirming zero remaining user-facing "Dashboard" text in `templates/`.

### Implementation notes

**Landed exactly as scoped — a straight literal `Dashboard` → `Calendar` text swap across the six files the plan identified, no surprises.** The cross-project Home dashboard's sidebar link (`templates/nav_sidebar_inner.html`'s fixed bottom-links block) already read "Home", not "Dashboard", before this stage — confirmed via grep before editing, left untouched per the plan's own note that "Home" isn't the word being renamed. `src/main.rs`'s two `.../dashboard` `<title>` blocks both collapsed to the identical `Calendar · Todo` for both the calendar view and the list view of each screen, matching the existing Tasks/Events convention (`Tasks · Todo` for both any of that section's views) rather than inventing a "Calendar List" variant — same title regardless of which of the two views is loaded, consistent with how Tasks/Events never distinguished their list/calendar title either back when Tasks/Events still had a calendar view.

**No `.rs` file needed a change** — confirmed via `grep -rn '"Dashboard' --include="*.rs" src/` returning zero matches before starting; every occurrence of the literal word lives in `templates/`.

**Verification actually performed**: `cargo build` (clean, identical pre-existing warning set to every prior stage — zero new warnings from a template-only change), `cargo fmt -- --check` (zero new drift — the tool's full output shows only the same pre-existing drift spots in `main.rs`/`calendar_subscriptions.rs`, files this stage never touched), `cargo test` (455 passed, unchanged), `task check` (clean). Repo-wide `grep -rn "Dashboard" templates/` returned zero matches after the edits. Per `CLAUDE.md`'s no-Playwright instruction, live verification was `curl` against a locally-run `cargo run` (`TODO_BIND=127.0.0.1:3905`, `TODO_AUTH_MODE=caddy` + `x-token-user-email` header, a throwaway SQLite DB under the scratchpad dir): confirmed `/web/dashboard` and `/web/dashboard/list` both render `<title>Calendar · Todo</title>`; created a throwaway project and confirmed its `/web/projects/:id/dashboard` page renders `<title>Calendar · Todo</title>` plus a `>Calendar<` sidebar link and a `>Calendar<` page heading, with the cross-project page's own sidebar still showing `>Home<` (unchanged). Did not verify client-side rendering in an actual browser (nothing client-side changed this stage, so there was nothing new to exercise there) — the user's own visual check of nav/title wording is still worthwhile before considering this stage fully done, per their standing instruction on prior stages.

---

## Stage 8 — Rename "Dashboard" to "Calendar" (full route/module rename)

Split off from Stage 7 at the user's explicit request when that stage began, since this half is a much larger blast radius (every internal link, every redirect target from Stage 6, `nav.rs`, this plan's own file, `CLAUDE.md`'s Web UI section) for comparatively low functional value — worth its own independently-landable stage rather than folding into Stage 7's display-only change.

At minimum, in scope for this stage:

- **Routes**: `/web/dashboard` → `/web/calendar` (and `/web/dashboard/list` → `/web/calendar/list`, `/web/dashboard/calendar` → ... — note the existing `.../dashboard/calendar` redirect path from Stage 6 becomes doubly confusing once "dashboard" itself is renamed; decide at the start of this stage whether that redirect's URL segment gets renamed too, or only the base path does) for the cross-project screen; the analogous `/web/projects/:project_id/dashboard[...]` → `.../calendar[...]` for the per-project screen.
- **Module names**: `src/web_ui/main_dashboard.rs` → `src/web_ui/main_calendar.rs` (or similar), `src/web_ui/project_dashboard.rs` → `src/web_ui/project_calendar.rs` (or similar) — confirm actual naming with the user before renaming, since "Calendar" alone may collide/confuse with `src/web_ui/project_events/` calendar-adjacent concepts or `calendar_subscriptions.rs`/`calendar_sync.rs` (unrelated Google Calendar sync feature — same word, different feature, already a potential source of confusion worth flagging to the user explicitly).
- **Struct/template names**: `MainDashboardRow`/`ProjectDashboardRow` and every `*Dashboard*Template` struct; `templates/main_dashboard/` / `templates/project_dashboard/` directory renames; the Stage-6 redirect handlers (`redirect_main_dashboard_calendar`/`redirect_project_dashboard_calendar`) need new names that still make sense once "dashboard" isn't the word for the old path anymore.
- **`nav.rs`**: `section_href`'s `SidebarSection::None => "dashboard"` path segment, and its doc comments referencing "Dashboard".
- **`CLAUDE.md`**: the Web UI section's route tables and prose describing `main_dashboard.rs`/`project_dashboard.rs`.
- **This plan file**: every stage's own prose above refers to "the Home dashboard"/"the per-project dashboard" throughout — out of scope to rewrite retroactively (historical record of what was actually built at the time), but worth a note here that post-Stage-8 readers should mentally substitute "Calendar" for "Dashboard" when the module/route names are cited literally.

**Confirm the exact new names (module/struct/route naming) with the user at the start of this stage** before renaming anything — this note is carried over from Stage 7's original draft, since naming is still genuinely open (e.g. avoiding collision with the unrelated `calendar_subscriptions.rs`/`calendar_sync.rs` Google Calendar sync feature).

**Files touched:** `src/main.rs` (route table), `src/web_ui/main_dashboard.rs` (renamed), `src/web_ui/project_dashboard.rs` (renamed), `templates/main_dashboard/*` (renamed dir), `templates/project_dashboard/*` (renamed dir), `src/web_ui/nav.rs`, `src/web_ui/mod.rs` (module declarations), `templates/teams/detail_page.html` and any other template referencing the old routes, `CLAUDE.md`.

**Verification:** `cargo build`, `cargo test`, `task check`; grep confirming zero remaining references to the old route paths/module names outside this plan file's own historical stage notes; live click-through confirming every internal link/redirect/bookmark (including the Stage-6 `.../calendar` redirect) still resolves correctly under the new names.

### Implementation notes (fill in before ending this stage)

---

## After this plan is complete

- Move the four folded-in bullets (row-actions ellipsis menu, Events-duplicate-parity, calendar highlight bug, calendar assigned-filter) out of `docs/issues_and_features.md` into `docs/archived/archived_issues_and_features.md`.
- Revisit the remaining "shrink the calendar view... put the month beside the list" bullet — reword or split it, since the drawer resolves the layout-competition problem it was originally about; the "shrink the grid" half may still be worth doing independently.
- Move this file itself (`docs/calendar-day-drawer-plan.md`) into `docs/archived/`.
