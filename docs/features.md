Ranked easiest → hardest (re-ranked 2026-08-15 after the item_series redesign completed — see docs/recurring-events-virtual-occurrences-rough-plan.md). Five items previously listed here were already implemented and have been moved to archived-features.md: "Simple lists shouldn't have assignment UI", "only show the current materialized occurrence of a task series in the flat task list" (the latter had been mistakenly re-added as open by a doc-sync commit after it already shipped — see that archive entry's correction note), "Need to be able to edit series in the UI" (fixed 2026-08-15), "Events shouldn't have any concept of 'completeness'" (fixed 2026-08-15), and "Need to be able to delete series in the UI" (fixed 2026-08-15).

1. Activity log should also include non-point completions and allow reversal. **Medium** — unrelated to the item_series work. Today `activity_log` is scoped to points/completion events on team-backed-project items only (CLAUDE.md's Points section); logging *every* completion (including personal-project and non-points team items) needs a new log-entry shape or a nullable-points variant of the existing one, plus reversal semantics for a completion that never had points to reverse (currently `reverse_entry` only ever un-awards points — un-completing the item itself is a separate, already-existing action).

2. Individual and Mass rescheduling ("skip current" without completing). **Medium-hard, but fully designed already** — see `docs/scheduled-catchup-plan.md`, which already resolved every open design question (cross-project screen vs. dashboard preset, non-recurring items land on "today, same time", Tasks-only for now). This is scoped as pure implementation against that doc, not a redesign — but it's still a new cross-project screen plus a new repo method plus a bulk-action pattern this codebase doesn't have a precedent for yet (closest analog: the CSV importer's best-effort-per-row model), so it's a real multi-file feature, not a quick win.

3. Due date should be secondary for tasks (mirroring Todoist's `scheduled_date`-primary model), and/or due date should follow the same recurrence rules as scheduling. **Hardest, explicitly deferred already** — see CLAUDE.md's Scheduled start/end section ("Deferred / partially implemented"): touches dashboard preset grouping/sorting, `list_due`/`prl items due`, and web-UI field-prominence ordering across every screen. Also tangled up with the still-unresolved "floating calendar date" display tension noted in the same CLAUDE.md section (bare dates anchor to an instant computed from the submitter's timezone rather than floating with the viewer) — worth resolving that design question first, since this feature would touch the same date-display code paths. Not scoped or planned in any doc yet; would need its own planning pass before implementation.

## unsorted items (to be sorted into above)

- In calendar views, show assigned user (First letter of first name) beside tasks and task series if assignment exists
- Put delete button in all details pages