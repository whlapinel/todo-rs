Ranked easiest → hardest (re-ranked 2026-08-15 after the item_series redesign completed — see docs/recurring-events-virtual-occurrences-rough-plan.md). Five items previously listed here were already implemented and have been moved to archived-features.md: "Simple lists shouldn't have assignment UI", "only show the current materialized occurrence of a task series in the flat task list" (the latter had been mistakenly re-added as open by a doc-sync commit after it already shipped — see that archive entry's correction note), "Need to be able to edit series in the UI" (fixed 2026-08-15), "Events shouldn't have any concept of 'completeness'" (fixed 2026-08-15), and "Need to be able to delete series in the UI" (fixed 2026-08-15). As of 2026-08-19, the "unsorted items" section's "Major change" entry (virtual/materialized occurrence unification) is fixed too, all four stages of `docs/unify-virtual-materialized-occurrences-plan.md` now complete — also moved to archived-features.md.

1. Activity log should also include non-point completions and allow reversal. **Medium** — unrelated to the item_series work. Today `activity_log` is scoped to points/completion events on team-backed-project items only (CLAUDE.md's Points section); logging *every* completion (including personal-project and non-points team items) needs a new log-entry shape or a nullable-points variant of the existing one, plus reversal semantics for a completion that never had points to reverse (currently `reverse_entry` only ever un-awards points — un-completing the item itself is a separate, already-existing action).

2. Individual and Mass rescheduling ("skip current" without completing). **Medium-hard, but fully designed already** — see `docs/scheduled-catchup-plan.md`, which already resolved every open design question (cross-project screen vs. dashboard preset, non-recurring items land on "today, same time", Tasks-only for now). This is scoped as pure implementation against that doc, not a redesign — but it's still a new cross-project screen plus a new repo method plus a bulk-action pattern this codebase doesn't have a precedent for yet (closest analog: the CSV importer's best-effort-per-row model), so it's a real multi-file feature, not a quick win. **Flag before implementing (found 2026-08-19, see `docs/unify-virtual-materialized-occurrences-plan.md`'s Context section): `scheduled-catchup-plan.md` predates the item_series redesign and is written against the retired item-level `recurrence` field, with zero series-awareness in its "overdue" query. As written, it would directly overwrite `scheduled_date` on a stale materialized series occurrence, desyncing it from the series cursor and reintroducing the "stuck series" bug class fixed 2026-08-19 (`issues.md`'s "major bug" entry). Add a `series_id IS NULL` guard to that query (trivial now that `items.series_id` exists) before implementing.**

3. Due date should be secondary for tasks (mirroring Todoist's `scheduled_date`-primary model), and/or due date should follow the same recurrence rules as scheduling. **Hardest, explicitly deferred already** — see CLAUDE.md's Scheduled start/end section ("Deferred / partially implemented"): touches dashboard preset grouping/sorting, `list_due`/`prl items due`, and web-UI field-prominence ordering across every screen. Also tangled up with the still-unresolved "floating calendar date" display tension noted in the same CLAUDE.md section (bare dates anchor to an instant computed from the submitter's timezone rather than floating with the viewer) — worth resolving that design question first, since this feature would touch the same date-display code paths. Not scoped or planned in any doc yet; would need its own planning pass before implementation.

## unsorted items (to be sorted into above)

- In calendar views, show assigned user (First letter of first name) beside tasks and task series if assignment exists
- Supersedes above but include above in this more broadly-encompassing feature: modify calendar to separate items to a per-day list below the month view instead of attempting to display items within the day. So within the month view, each day will show some kind of minimal count indicator and perhaps an abbreviated hint of what each item is, but clicking on the day will display the fuller list for that day.
- Put delete button in all details pages
- Series, Tasks, and Events list rows should have 'duplicate' action -- '<name>' => '<name> (copy)' -- folded into vertical elipses along with 'edit' and 'delete' and 'assign'
- Need a pop-up dialog component for all of these.
- Ctrl+click or tap+hold should select row, allowing for bulk actions
    - "delete-selected"
    - "reschedule-selected"
    - "assign-selected"
- Allow copying a task or event to another project
- Consider change such that delete will mark a delete column true rather than actually deleting. This allows for undo within a certain timeframe and easy-viewing of recent. BUT, I'm not sure whether this is a good pattern or not and would like to know tradeoffs compared with the standard approach.
- Allow admin to complete tasks not assigned to them
- Make dashboard a single cross-project page for the user rather than one dashboard per-project - each item should clearly indicate which project it's from
    - OR, maybe keep project dashboards but add a main cross-project dashboard
    - Main dashboard will show:
        - All events for all projects 
            - (materialized and virtual occurrences of event series)
        - All tasks assigned to me for all projects + all tasks for personal projects (i.e. no team assigned)
            - (materialized and virtual occurrences of task series assigned to me)
- Show assignee in tasks list as well
- Add metadata fields to item: created_at, created_by, deleted_at, deleted_by (instead of actually deleting, we tag for deletion to allow for easily recovering deleted items) and have a TTL after tagging for deletion, after which the item is actually deleted from the DB.
- Add priority to task items and task series - then modify sorting to sort first by priority, then by due date
- ~~Major change: I want virtual occurrences to look the same as materialized occurrences, except that I want to carry over the UI from virtual rows (e.g. display current if current, and skip action is available). This implies that skipping should be available for materialized occurrences, which means we need to essentially delete the occurrence and skip it in a single pass for that path. Additionally and more broadly, I want to materialize only when necessary, i.e. when marked complete, or when the occurrence is actually modified in some other way. So we need to have a way of viewing details for a virtual occurrence that looks the same as materialized occurrences - just use the series id and occurrence date instead of the item id for the URL. I want the distinction between virtual and materialized to be fully hidden from the user. The user should be able to see clearly when a task is from a series (again, I want a link to series from task details), but not whether it is materialized or not (unless they are observant enough to deduce from URL differences).~~ Fully implemented 2026-08-19 — folded together with `issues.md`'s former ranked item 2 (same request) and staged in `docs/unify-virtual-materialized-occurrences-plan.md`, all four stages (A: `items.series_id` + unified occurrence-listing; B: skip/unskip unification + series link; C: deferred materialization + virtual occurrence detail page; D: collapsed duplicated rendering) now complete — moved to archived-features.md.